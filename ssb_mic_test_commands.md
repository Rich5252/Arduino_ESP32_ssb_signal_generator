# ssb_mic_test.ino — Serial Command Reference

Send any of these single characters over the serial monitor (921600 baud). Each command prints a `->` confirmation line showing the new state.

## Signal source

| Key | Effect |
|---|---|
| `t` | Two-tone test signal (700/1900Hz by default) |
| `s` | Single clean tone (1000Hz) — isolates DSP/RF chain from mic-side artifacts |
| `m` | Live mic input |
| `p` | Envelope step test — 4Hz square wave direct to PWM, carrier held fixed. For measuring the analog reconstruction filter's step response with a scope, independent of everything else in the chain |
| `y` | FM isolation test — pure sinusoidal frequency modulation (1200Hz mod, 3000Hz peak deviation), envelope held fixed, bypasses `ssb_dsp_process_sample()` entirely. Isolates the AD9851/SPI/delay-line chain from the Hilbert/DSP math |
| `h` | AM isolation test — pure sinusoidal amplitude modulation (1200Hz mod, 100% depth), `freq_dev_hz` locked at 0, also bypasses `ssb_dsp_process_sample()`. Isolates the RSET/PWM/analog-filter/transistor path |
| `w` | Sine-chirp test — logarithmic 20Hz–20kHz sweep over 5s direct to the envelope/PWM output, with a synced square-wave reference on pin13 and a 30ms mute/sync marker at each restart. See "Sine-chirp test mode" below |
| `T` | Steps the two-tone pair through `TWOTONE_BAND_PRESETS` (`test_signals.cpp`), low to high center frequency: 300/500, 700/900, 1500/1700, 2500/2700, 3500/3700Hz (all 200Hz spacing, each a clean single-point probe), then the original 700/1900Hz pair (1200Hz spacing, kept last for reference — its wide spacing averages alignment across a much bigger span, so it's not directly comparable to the other five), wrapping around. Switches into two-tone mode too if not already there. For sweeping the pair across the audio band without a recompile, to empirically map how much relative-delay retuning each band needs — see the group-delay-equalizer band-mapping note below. **The 300/500 pair specifically has a real, separate opposite-sideband image issue below ~400Hz — see the 2026-09-05 entry below "Is `eq`'s IMD benefit..." section, not a `g`/`a`/`A`/`eq`/`R` effect** |
| `R` | Steps the two-tone pair's amplitude ratio through `TONE_RATIO_PRESETS` (`test_signals.cpp`): -3, -2, -1, equal (0dB, today's default), +1, +2, +3dB — tone2 relative to tone1, with tone1+tone2's combined peak held constant across every step (no new clipping risk in either direction). Switches into two-tone mode too if not already there. Added 2026-09-04, confirmed the same day to reproduce `eq`'s IMD benefit directly — see the dated entries in "Is `eq`'s IMD benefit the highpass or the presence boost?" below |

### Sine-chirp test mode (`w`, `AUDIO_SRC_CHIRP`)

Added to characterize the envelope/PWM (RSET) analog reconstruction filter's transfer function against a 2-channel ADC-based TF measurement rig, replacing the need to infer the filter's shape indirectly from two-tone/step-response artifacts.

`w` starts a logarithmic sweep from `CHIRP_F0_HZ` (20Hz) to `CHIRP_F1_HZ` (20kHz) over `CHIRP_SWEEP_SEC` (5s), direct to the envelope/PWM output, repeating continuously. A `CHIRP_MUTE_SEC` (30ms) silence at the start of every repeat acts as a sync/restart marker the measurement rig can trigger on. A square-wave reference — high while the chirp's own instantaneous sine is ≥0, forced low during the mute period — is driven on `CHIRP_REF_GPIO` (pin39, moved from pin13 2026-09-02 — see the GPIO13 section below) for the rig's reference channel. All constants live in `config.h`.

Unlike `p`/`y`/`h` (which bypass only `ssb_dsp_process_sample()` and still run on the normal `SAMPLE_RATE_HZ` (16kHz) full-tick pipeline), chirp mode bypasses ADC, DSP, `envelope_floor`, relative delay, AD9851, and normal diagnostics, and runs on **every** fast tick, i.e. the full `ENVELOPE_INTERP_FACTOR x SAMPLE_RATE_HZ` (64kHz) rate `envelope_interp.h`'s fast-tick timer already runs unconditionally at. This is required, not a convenience: 16kHz's own 8kHz Nyquist can't represent a 20kHz sweep.

**DC operating point — `u`/`j`/`i`/`k`/`D` still apply, master gain does not.** The offset/scale mapping (or predistort LUT if `D` is on) is deliberately the one pipeline stage chirp mode does NOT skip — every other bypassed stage would corrupt or be irrelevant to the raw filter TF, but this one sets WHERE on the duty range the sweep sits, which is exactly what you need to sweep to characterize the filter/BS170 gate across different bias points (e.g. testing whether the TF changes at the low vs. high end of the duty range). Master gain (`+`/`-`) does NOT do this — it only scales the swing depth around `AM_TEST_DEPTH`'s fixed mean (same convention `AMTEST` uses), so it changes how hard the filter is driven without moving the DC point at all; seeing no TF change between high/low gain is the expected result, not a null finding. Use `u`/`j` to shift the range and `i`/`k` to widen/narrow it instead.

**2026-09-02, reported and fixed: the `'w'` reply's own `serial_reply()` buffer was too small.** Adding the `'g'` state to the `'w'` confirmation text pushed that one reply to ~500 bytes, well past `serial_reply()`'s internal `char buf[256]` — `vsnprintf()` silently truncated it, which is exactly why the trailing `\r\n` went missing (the truncation cut it off) rather than anything electrical or timing-related. Fixed two ways: bumped the buffer to 512 bytes (comfortable headroom over every current reply in the file) and added a `Serial.printf()` warning on any future truncation, so it can't go silently unnoticed again the way this one did. Also trimmed the `'w'` reply text itself down to ~216 bytes for margin. The reported follow-on symptom ("second w gives no response") was initially explained away as the same pre-existing `src != TARGET` guard every other source-switch command uses (`t`/`s`/`m`/`p`/`y`/`h`) - technically correct, but the wrong call for `'w'` specifically. Reconsidered once the user pushed back: `'w'`'s own comment already documents that it's supposed to restart the sweep from t=0 on every press, and the new `'g'`-toggle A/B workflow gives a real, concrete reason to actually want that while already mid-sweep - without a restart, toggling `'g'` leaves you waiting up to the full `CHIRP_SWEEP_SEC` (5s) for the next natural mute/sync marker instead of an immediate, cleanly-triggered restart. **Fixed by dropping the guard for `'w'` specifically** (`c == 'w'` instead of `c == 'w' && src != AUDIO_SRC_CHIRP`) - it's now the one source-switch command in the file where re-sending it while already in that mode is a deliberate, useful "restart now" action rather than a no-op, and the code comments explain why it's an intentional exception to the rest of the file's convention rather than an inconsistency.

**2026-09-02, `g` (group-delay equalizer) now also applies, on request — to directly measure how well `envelope_gdeq` compensates the analog filter's group delay, not just characterize the raw filter.** `envelope_gdeq_process()` is called on `chirp_envelope` right after `test_signals_generate_chirp()`, before the offset/scale/predistort mapping — same insertion order as the normal pipeline. It shares the same live `'g'` toggle as everything else (the function internally no-ops when disabled, so no separate gating was needed), so the workflow is: sweep with `g` off for the baseline/uncompensated TF, toggle `g`, re-run `w` for the compensated sweep, and compare group delay (phase slope vs frequency) between the two on the TF rig. The `'w'` serial reply now reports current `g` state so it's visible at a glance which sweep is running. `envelope_floor` stays skipped — it's a hard near-zero clamp for mic-path silence, not something relevant to measure through.

**pin conflict**: `CHIRP_REF_GPIO` reuses the same physical pin as `TIMING_DEBUG_GPIO_CMD` (the serial-activity-correlation marker toggled every `loop()` iteration — see `config.h`'s `TIMING_DEBUG_GPIO_CMD`/`ADC_ISR_DEBUG_PIN_ENABLED` history). `CMD_DEBUG_PIN_ENABLED` (config.h, default **0**) gates that marker off so chirp's reference square wave isn't corrupted by it; flip it back to 1 (and expect chirp's reference output to go dark) if that marker is needed again instead.

## Phase/envelope relative timing

| Key | Effect |
|---|---|
| `[` | Decrease relative delay by 0.05 samples (5µs @ 10kHz, 3.125µs @ 16kHz) |
| `]` | Increase relative delay by 0.05 samples |
| `;` | Decrease relative delay by 0.01 samples — finer step, added 2026-09-06 |
| `'` | Increase relative delay by 0.01 samples — finer step, added 2026-09-06 |

Signed, bipolar, fractional (interpolated between ring-buffer samples). **Positive** delays the *phase* path (AD9851) relative to envelope. **Negative** delays the *envelope* path relative to phase. Range roughly ±6 samples. Current best-known value: **-0.20 to -0.25 samples**.

**2026-09-06: added `;`/`'` as a finer-step pair alongside `[`/`]`** — same coarse/fine convention as `+`/`-` vs `.`/`,` for master gain (`DELAY_FINE_STEP_SAMPLES`, `relative_delay.h`). Prompted directly by the candidate-B bench result above: both the default and candidate-B gdeq configs need their delay chosen from within a sub-0.1-sample compromise window (2.00–2.10 samples) where different two-tone IMD orders trade off against each other — finer than two presses of the plain 0.05-sample `]` could resolve. `;`/`'` sit next to `[`/`]` on a US QWERTY layout; `.`/`,` and `{`/`}` (the natural shifted `[`/`]` pair) were both already taken by master gain and the freq_dev slew limiter respectively.

## PWM duty-cycle range (envelope → RSET mapping)

Separate from master gain — this only remaps envelope's own output into a duty-cycle range, controlling where the signal sits relative to the BS170 gate's characterized sweet spot (~2.3V ±0.75V).

| Key | Effect |
|---|---|
| `u` | Raise duty range offset by 2% |
| `j` | Lower duty range offset by 2% |
| `i` | Widen duty range span by 2% |
| `k` | Narrow duty range span by 2% |

Default: offset 0.20, scale 0.90 → duty range 20%–100% (actually caps at ~92% due to the scale).

## Envelope group-delay equalizer

| Key | Effect |
|---|---|
| `g` | Toggle two cascaded first-order digital all-pass sections on the envelope path, fitted to flatten the *original* (non-Bessel) 2-pole Sallen-Key filter's group-delay dispersion — as of 2026-09-03, fit directly against a real-hardware TFA phase sweep over 100–8000Hz (was: LTspice sim, 100–4300Hz; see `ENV_GDEQ_A1`/`ENV_GDEQ_A2` in `envelope_gdeq.h` for the full derivation). Goal: get that filter's better stopband rejection without its dispersion-driven IMD penalty, instead of the Bessel filter's compromise. **Not yet IMD-validated on real hardware — see the dated entries below, including a correction to this project's own first same-day dispersion report.** |

Off by default. Enabling it pushes the envelope path's overall delay up by ~265µs (it can only add delay, not remove it) — re-tune `[`/`]` from scratch afterward; theoretical starting point is roughly **+2.65 samples**, a different regime from the Bessel filter's -0.20 to -0.25 samples.

**2026-09-03, `g` wired into the `'w'` chirp/sweep test path so the TFA can measure it directly.** `envelope_gdeq_process()` is now called unconditionally inside the `AUDIO_SRC_CHIRP` fast-path in `dsp_task` (`ssb_mic_test.ino`), in the same pipeline position as the normal full-tick path (before the offset/scale/predistort DC mapping) — it already no-ops internally when disabled, so no extra gating needed. Workflow: toggle `g`, press `'w'` to force an immediate fresh synced sweep (see next entry), read the TFA. Two bugs surfaced and were fixed getting here: (1) `serial_reply()`'s stack buffer (`char buf[256]`) was silently truncated by the longer `'w'` reply text once it grew to include `g`'s state — bumped to 512 bytes, reply trimmed to ~216 bytes, and a truncation warning added for future visibility; (2) `'w'` originally still guarded on `src != AUDIO_SRC_CHIRP` like every other source-select key in this file, so a second `'w'` press mid-sweep (needed for the A/B `g` workflow) did nothing — that guard was deliberately removed for `'w'` only (all other keys' guards untouched), so `'w'` now always restarts the sweep, even mid-sweep, on every press.

**2026-09-03, real-hardware TFA measurement (unwrapped phase, `g` off vs. on, sine-chirp sweep) — decisive gap from the LTspice-fit design prediction.** Phase data smoothed (Savitzky-Golay, window 31 ≈700Hz span, polyorder 3) before differentiating to group delay (`tau = -(1/360) dPhase/dFreq`). Over the design's own 100–4300Hz target band, at the currently-active config (`ENV_FILTER_VARIANT = ENV_FILTER_PNP_BC327_ATTN`, `SAMPLE_RATE_HZ = 16000`, i.e. `ENV_GDEQ_A1 = ENV_GDEQ_A2 = 0.622515`):

| | design prediction (`envelope_gdeq.h`, 2026-09-01 refit) | measured (this entry) |
|---|---|---|
| analog alone, p-p dispersion | 41.1µs | **146.1µs** |
| equalized, p-p dispersion | 13.7µs | **147.2µs** (essentially unchanged) |
| mean added delay | ≈131.6µs (2.105 samples @ 16kHz) | **≈40.3µs** (~31% of predicted) |

Sub-band breakdown (matters — the overall figure is not uniform across the band): 100–1000Hz shows a real, directionally-correct improvement (29.2→21.2µs p-p, ~28% reduction); 1000–2000Hz is flat (41.3→41.2µs); 2000–3000Hz shows a small improvement (69.0→66.5µs); 3000–4300Hz is flat-to-slightly-worse (146.1→147.2µs) and dominates the overall band figure since it's the widest and highest-magnitude sub-band. A residual-phase-noise check (raw phase minus the smoothed curve) shows RMS residual rising from ~0.4° at 100–1000Hz to a peak of ~2.5° at 3000–4300Hz — close to the analog filter's own corner, where attenuation is heaviest and measurement SNR is worst — so the apparent lack of correction specifically at the top of the band is confounded by degraded phase measurement there, not necessarily pure equalizer failure. No magnitude/coherence channel was captured in this measurement, so that confound can't be fully separated from a real equalizer shortfall with this dataset alone.

**Working interpretation (at the time):** the equalizer is doing *something* real and directionally correct at the low end of the band, but is well short of its LTspice-fitted design target overall, especially in the upper band where it was supposed to do the most work. **See the correction directly below — this interpretation turned out to be substantially a smoothing-window artifact, not a real equalizer failure at 100–4300Hz.** Raw TFA data: `Group_delay_off__on_F_Phase.txt` (user-supplied, tab-separated freq/phase, 'g' off then 'g' on, 0–24kHz).

**2026-09-03, CORRECTION to the entry above: the 31-point smoothing window was too fine for this real (noisier, denser) hardware sweep, and overstated the dispersion.** Prompted by a second request the same day: the user reported the TFA front end previously had a loading issue, now fixed by adding Hi-Z buffers, and asked for the compensation to be refit against the real data and extended to 8000Hz (`freq_dev_hz` excursions have been observed out to `MAX_FREQ_DEV_HZ`). While doing that refit, re-examined the smoothing choice: the 31-point/~700Hz window used above leaves a residual (raw minus smoothed) with 0.5–2.6° RMS depending on band — enough, once differentiated, to plausibly produce the tens-of-µs swings seen above ~2kHz on its own, not real filter structure (a real 2-pole-plus-buffer-stage filter's phase shouldn't wiggle that fast). Re-ran the SAME measured `g`-off/`g`-on curves (not a model — the actual measured phase in both states) at a wider window justified by a convergence check (stable from ~181 points/~4.2kHz span onward):

| Band | `g` off (measured) | `g` on (measured, then-active 0.622515/0.622515 coeffs) | |
|---|---|---|---|
| 100–4300Hz, 31-pt window (original entry above) | 146.1µs p-p | 147.2µs p-p | "no improvement" |
| 100–4300Hz, 181-pt window (this correction) | 36.6µs p-p | 14.3µs p-p | **real ~61% reduction, close to the 41.1→13.7µs LTspice prediction** |

So the *old* coefficients were doing close to what they were designed to do within their own 100–4300Hz fit band all along. The original "no improvement" finding above was substantially a smoothing-window artifact on noisy real data, not a genuine equalizer failure at those frequencies — left uncorrected above (not edited away) per this project's convention; this entry is the correction. This does **not** touch the separate, independently-confirmed 2026-09-01 real-hardware IMD result in `group_delay_fit_notes.md` (that measurement's repeatable delay-swept 3rd-order IMD numbers never depended on this phase-smoothing question) — only the group-delay/TFA read of things changes.

**2026-09-03, refit: compensation now fit directly against real-hardware TFA data, band widened 100–4300Hz → 100–8000Hz.** Full method, the smoothing-window convergence check, and the fit result live in `group_delay_fit_notes.md`'s matching entry — short version: `a1=0.026173`, `a2=0.236810` (was `0.622515`/`0.622515`, LTspice-only), now compiled into `envelope_gdeq.h` for `ENV_FILTER_PNP_BC327_ATTN` @ 16000Hz. Predicted 100–8000Hz: analog alone 71.2µs p-p → equalized 18.5µs p-p (~3.9x); mean added delay ≈125.4µs (**2.006 samples @ 16kHz**, down slightly from the old fit's 2.105). The OLD coefficients, checked against this same real analog curve, still track fine inside their own 100–4300Hz band but blow up to 449µs p-p once evaluated past it — confirming a real re-fit was needed, not just a wider claimed range. **Still not IMD-validated**: the 2026-09-01 real-hardware finding that `g` made 3rd-order IMD 6–10dB worse was against the OLD coefficients and doesn't automatically carry over to this refit — it directly addresses two of that finding's four suspected causes (narrow fit window; possibly the DC-bias measurement inconsistency the Hi-Z buffers targeted) but not the other two (insertion loss; the `'I'` confound). `g` stays off by default until the same real-hardware IMD delay-sweep procedure is re-run against these new coefficients — re-tune `[`/`]` starting near +2.0 samples when that happens.

**2026-09-03, group-delay side confirmed on real hardware — new coefficients flashed to the board and re-measured.** Source: `Group delay off  on F Phase Refit on TF meas.txt`, a genuine new TFA sweep (not a re-analysis of the earlier file). Measured, 100–8000Hz: analog alone **71.3µs p-p** (vs. 71.2µs the previous session — good repeatability, the Hi-Z buffers are holding up) → new coefficients **20.3µs p-p** (~3.5x), mean added delay **125.2µs (2.003 samples @ 16kHz)**, within 1µs of the fit's own 125.4µs/2.006-sample prediction. The measured curve tracks the predicted one to ~2µs RMS across the band — the numeric fit is behaving on the bench the way the model said it would. **Data-provenance note:** this file's two phase columns came out swapped relative to the earlier off-then-on convention (caught because taking them at face value implies a negative added delay, which an all-pass network can't produce); corrected by matching each column against the earlier measured/predicted curves, which line up almost exactly once swapped — worth checking the TFA channel routing before the next sweep. **This confirms the group-delay/dispersion prediction, not IMD** — the 2026-09-01 finding that the old coefficients hurt 3rd-order IMD involved mechanisms (insertion loss, the possible `'I'` confound) a group-delay sweep can't see either way. `g` stays off by default until the IMD delay-sweep is re-run against these coefficients specifically.

**2026-09-03, later same day: promising real-hardware IMD result with the new coefficients, at a delay never tested before — full writeup in `group_delay_fit_notes.md`.** Optimum `relative_delay` with the new coefficients is now ~4 samples (previously untested territory — the old-coefficient sweep only went up to 2.00 and got worse from there, but that was a different, steeper filter) and, notably, now *consistent* between two-tone and mic-noise stimuli, which the old coefficients never achieved. At that delay, higher-order two-tone IMD products are visibly reduced with `g` on. **Not a gain-compensation effect** — `ssb_allpass1_t` is a true all-pass, unity-magnitude by construction, so `g` cannot be touching the envelope's amplitude spectrum; the improvement is much more likely genuine delay-alignment benefit in a region the earlier sweep never reached. Still short of a full re-run of the 2026-09-01 delay-sweep/IMD-table methodology around this new ~4-sample region — that's the recommended next step before flipping `g`'s default. Separately, a real amplitude+phase TF measurement (`TF meas.txt`, same sweep as the first group-delay file) confirmed the analog filter's insertion loss directly on hardware: -0.45dB @700Hz, -4.52dB @3100Hz, -8.75dB @4300Hz, **-21.7dB @8000Hz** — tracking the LTspice prediction well, especially at the top end. Not yet compensated; a true magnitude correction needs a real gain-shaping filter (an all-pass can't do it) and raises real headroom/noise-floor tradeoffs at the frequencies that matter most — see `group_delay_fit_notes.md` for the full discussion.

## Envelope magnitude (insertion-loss) equalizer

| Key | Effect |
|---|---|
| `a` | Toggle shelf 1 (fc=2500Hz, +6dB) — a high-shelf digital biquad on the envelope path (`ssb_shelf_biquad_t`, `envelope_ampeq.h`/`.cpp`), partially compensating the analog reconstruction filter's own measured high-frequency gain roll-off — the magnitude counterpart to `g`'s group-delay equalizer. **Magnitude side and real IMD benefit confirmed on real hardware 2026-09-03 — see the dated entry below. It also measurably reintroduces group-delay dispersion `g` had flattened, a real, quantified trade-off.** Currently this project's recommended config: `a` ON. |
| `A` | Toggle shelf 2 (fc=6000Hz, +10dB) — a second, independent high-shelf stage added 2026-09-04, extending correction further toward 8000Hz (Nyquist at this Fs). Independent of `a` — either can be on with the other off. **Validated on real hardware 2026-09-04: real magnitude gain at 8000Hz, but group-delay dispersion roughly doubles again, and two-tone IMD with both `a`+`A` on came back marginally WORSE than `a` alone (in this still-unrefit-`g` state) — see the dated entries below.** Currently recommended OFF pending a `g` refit; kept available for A/B testing (2026-09-04 update: preliminary reports with `a`+`A` and `eq` off suggest it may help HF IMDs specifically in that combination — see the newest dated entry below, still being characterized). |

Both off by default. Toggling `a`/`A` while sweeping `w` shows each correction directly on the amplitude channel (same trick `g`'s wiring into the chirp path already uses for phase). `g`, `a`, and `A` are all independent and can be toggled in any combination — order doesn't matter between LTI stages, though each shelf does have its own group-delay contribution that isn't folded into `g`'s fit yet (see the dated entries below for how large).

**2026-09-03, `envelope_ampeq` built in response to the IMD/insertion-loss finding above.** Before picking a filter shape, checked whether the existing pre-Hilbert presence EQ (`e`, currently +4dB/2200Hz/Q1, a peaking/bell biquad) could do this job — computed directly: it's back to +0.00dB by 8000Hz, so a peaking filter can't touch a loss that keeps climbing monotonically to the top of the band. Added a public `ssb_shelf_biquad_t` high-shelf primitive to `ssb_dsp.h`/`.c` instead (named to avoid colliding with `ssb_adc_filter.h`'s own, differently-shaped `ssb_biquad_t` — the first version used that name and failed to link; renamed to fix) (separate from the private peaking-EQ biquad `e` already uses). Chosen shelf (fc=2500Hz, +6dB, S=1) recovers most of the measured loss through 500–3100Hz, meaningfully reduces it at 4300Hz, and deliberately leaves most of the 8000Hz loss uncorrected — see `group_delay_fit_notes.md`'s matching entry for the full net-correction table and rationale. Wired into both the main `dsp_task` pipeline and the `AUDIO_SRC_CHIRP` path (right after `g`'s own calls in each), plus a new `env_ampeq_enable` trailing field in `PersistentSettings` (off by default) and the `P`/preset-load paths. **Status: implemented, not yet bench-validated** — next step is a `w` sweep with `a` ON to check the amplitude channel against the predicted table, then real IMD testing.

**2026-09-03, later same day: `a` validated on real hardware (`g`+`a` sweep, `ga_Trial1_TF.txt`).** Magnitude tracks the predicted net-correction table to within a few tenths of a dB at every checked frequency. Real IMD feedback: a genuine positive effect — balances upper/lower sideband IMD and brings the LF-region higher-order IMDs down to match the HF-region ones. **But it also measurably reintroduces group-delay dispersion**: p-p dispersion over 100–8000Hz goes from 19.4µs (`g` alone) to 81.7µs (`g`+`a`, ~4×), while mean absolute delay barely moves (198.6µs → 197.2µs) — matches the shelf's own analytically-computed group delay (p-p ≈65µs, mean ≈0.4µs) closely enough to confirm the mechanism: the shelf isn't phase-transparent, and `g`'s fit has no way to know about it (fit before `a` existed). Chart: `gdeq_ampeq_delay_chart_v4.html`. Not a contradiction of the IMD result — the amplitude benefit is currently winning — but worth watching if `a`'s gain is ever increased, and worth re-running the `g`-alone ~4-sample IMD optimum search with `a` also on.

**2026-09-04: `a` extended to two cascaded shelf stages, pushing correction further toward 8000Hz.** Requested specifically to test whether closing more of the 8000Hz gap improves the higher IMDs further. Added `ENV_AMPEQ_SHELF2_FREQ_HZ=6000Hz`/`ENV_AMPEQ_SHELF2_GAIN_DB=+10dB` cascaded after the existing shelf, still under the one `'a'` flag (no new key). Net correction at 8000Hz improves from -15.70dB (shelf1-only) to -5.70dB; 500–1900Hz essentially untouched; 3100Hz cleared to -0.02dB. **Cost: shelf2's own analytic group delay is 125.4µs p-p — larger than shelf1's 64.9µs — and stacks with it (148.6µs p-p combined)**, a ~120µs swing across exactly the two-tone/IMD band. This is not a fixable-by-better-shelf-design problem: a causal minimum-phase magnitude filter's gain and phase are locked together (same reason gdeq's own filters had to be built unity-magnitude to get delay correction for free) — more high-frequency gain recovery unavoidably costs more nearby delay distortion. **`g` has not been refit against this** — its current coefficients only ever saw the bare analog filter, and are already known (previous entry) to pass shelf1's dispersion straight through; adding shelf2 without a refit will make the delay side worse still. **Not yet bench-tested at all** (analytic prediction only). Recommended sequence: `'w'` sweep first to measure the real combined magnitude+phase response, then decide whether a `g` refit (against analog+shelf1+shelf2 combined) is worth doing before drawing any IMD conclusions — testing IMD with the delay side unrefit would conflate "more amplitude correction" with "more delay dispersion" and not answer the question cleanly. Full analysis and net-correction/delay tables in `group_delay_fit_notes.md`.

**2026-09-04, later same day: shelf2 validated on real hardware (`ga_Trial2_TF.txt`) — prediction confirmed, IMD marginally worse, reverting to shelf1-only for now.** Magnitude at 8000Hz: real net -16.03dB (shelf1-only) → -9.30dB (shelf1+shelf2), a genuine **+6.7dB** improvement, somewhat short of the +10dB predicted. Group delay: p-p dispersion over 100-8000Hz **roughly doubled** (75.7µs → 157.5µs, recomputed with one consistent pipeline for a clean comparison), tracking shelf2's own predicted delay curve closely in shape except at 8000Hz itself, where the real jump (+85µs) exceeded the analytic prediction (+62µs) — real hardware shows more top-end delay growth than the idealized shelf model right at the band edge. Mean delay unchanged either time (~197µs). **User's real IMD report: marginally worse with shelf2 on** — read against the numbers above, ~6.7dB of amplitude gain and ~82µs of extra dispersion landed close to a wash, neither effect clearly dominating, which is itself informative (supports this project's working model that both amplitude symmetry and delay flatness matter for IMD, not just one). **Decision: revert to shelf1-only as the better current real-world compromise while recording a wider IMD comparison** — this is a config choice, not a code removal; shelf2 stays in the firmware (same `'a'` flag) for later re-testing, ideally after a `g` refit against the new combined phase response. Chart: `gdeq_ampeq_delay_chart_v6.html` (real Trial1 vs. real Trial2). Full writeup in `group_delay_fit_notes.md`.

**2026-09-04, later still: preliminary report with the independent `A` toggle — a mixed bag, `eq` state seems to matter.** User's own words: "A mixed bag so far. Reduces HF IMDs with eq off but otherwise not a lot of change." First real use of the new independent toggle for a wider comparison, still being characterized. A candidate mechanism (`A` possibly helping the envelope path "gate" the `freq_dev` null transient faster/more accurately, rather than preventing it) is worked through in `group_delay_fit_notes.md`'s matching entry — NOT yet confirmed. Still needed before this is a solid finding: which specific IMD products counted as "HF", whether `g` was on/off during these runs, and a same-conditions `eq`-on comparison to confirm the contrast is real and not run-to-run noise.

**2026-09-04, confirmed on the bench: nulls scope quicker and deeper with `a`, quicker/deeper still with `A`** — exactly the candidate mechanism above predicted. User's own follow-up: "Maybe we should refine the grp delay again to optimise that for the a+A case." A candidate `g` refit was worked out (reconstructed from the existing `ga_Trial2_TF.txt` sweep via LTI subtraction of the current gdeq's known phase, validated by self-consistency — no new sweep needed for this step): best achievable is **157.5µs → 72.6µs p-p** (~2.2×, new coefficients a1=a2≈-0.1391), well short of the original ~4× flattening, because the analog+shelf1+shelf2 curve now has two interior extrema plus a steep near-Nyquist edge (shelf2's own signature) that a cascaded first-order all-pass can't fully track — confirmed via grid search, global optimization, and trying 3-4 sections (none help meaningfully). **Important: the improvement is NOT uniform** — it fixes 8000Hz dramatically but adds 24-66µs of delay at 500-3100Hz (the two-tone's own fundamentals) relative to what's flashed now, so a smaller p-p number isn't automatically better for real IMD — needs a bench test. Also still open: whether to keep gdeq's coefficients fixed (a compromise), make them switch automatically with `a`/`A`'s state, or treat this as a manually-swapped value for `a`+`A` bench sessions specifically. Full numbers, the trade-off table, and chart (`gdeq_refit_a_A_candidate_v7.html`) in `group_delay_fit_notes.md`.

**2026-09-04, same day: bench IMD result — no measurable difference, candidate not adopted.** Real two-tone A/B (`aAgdeq_optimise_comparison.xlsx`, `g`+`a`+`A` all on, default coefficients vs. candidate) came back IMD-indistinguishable — every product with a clean margin above the noise floor moved by under ~1.1dB either way, and the larger-looking swings all traced to products sitting at/below the noise floor (meaningless) except one 750Hz mid-band reading, which has its own history (2026-09-02 interp-tick entries) of tracking timing/PSU effects rather than filter coefficients and isn't being counted as evidence. So despite predicting ~2.2x better p-p dispersion, the candidate buys nothing measurable on real IMD — `ENV_GDEQ_USE_AA_CANDIDATE` stays at its default (0), not recommended for use. Full breakdown in `group_delay_fit_notes.md`'s matching entry.

**2026-09-04, same day: the a+A candidate landed in `envelope_gdeq.h`, off by default.** The open question above (fixed/automatic/manual) resolved as manual: a new compile-time flag, `ENV_GDEQ_USE_AA_CANDIDATE` (0 = default, keeps the existing `a1=0.026173`/`a2=0.236810`; 1 = the a+A candidate, `a1=a2=-0.139115`), sits right above the `#if` block picking the active coefficients for `ENV_FILTER_PNP_BC327_ATTN` @ 16000Hz. Flip it to 1 and reflash only when specifically bench-testing with **both `a` and `A` on** — leaving it at 1 for `g`/`g`+`a`-only testing will make those worse, since the candidate was fit for the a+A curve specifically. **Not yet validated on real hardware for the a+A case** — still a reconstructed-curve candidate, not confirmed against a fresh direct sweep. Mean added delay is close to the default's (124.4µs vs. 125.4µs, ~2 samples either way), so switching between the two should only need a small `[`/`]` nudge. Next steps before trusting it: a real `g` OFF + `a`+`A` ON TFA sweep to confirm the reconstruction, then the actual two-tone IMD comparison — the trade-off is real but non-uniform (see the entry above), so the smaller p-p number alone doesn't settle whether it's a net win. Full derivation and the "why manual over automatic" reasoning in `group_delay_fit_notes.md`'s matching entry.

**2026-09-05: the real-hardware TFA sweep of the candidate came back — and it doesn't show the predicted improvement.** `gaA_Trial3_Optimised_gd_TF.txt` processed with the same validated pipeline as Trial1/Trial2: p-p came back **154.8µs**, essentially identical to Trial2's 157.5µs (default coefficients) and nowhere near the candidate's own predicted 73.0µs. Checked the raw phase directly, not just the derived group delay: Trial3's phase matches Trial2's to within ~1 degree across the whole 100-8000Hz band — far too close to be the candidate's genuinely different `a1=a2=-0.139115` coefficients, which should produce tens of degrees of differential phase by 8000Hz. **This looks like `ENV_GDEQ_USE_AA_CANDIDATE` was not actually 1 in the firmware that produced this sweep** — worth double-checking the flag is set, doing a clean rebuild, confirming reflash, and re-running `'w'` before drawing any conclusion about the candidate. Separately, Trial3's magnitude reads ~6-7dB below Trial2's from 300Hz up to ~5500Hz, narrowing near 8000Hz — doesn't fit an ampeq shelf-state explanation (shelves shouldn't touch the lower part of that range at all) and looks more like a master-gain or measurement-chain difference between the two sessions than anything DSP-related; worth checking both sweeps used the same settings. Chart: `gdeq_trial3_candidate_check.html`. Full numbers in `group_delay_fit_notes.md`'s matching entry.

**2026-09-05, confirmed: the flag wasn't `1` in the flashed build.** User confirmed the diagnosis directly - "Flag wasnt '1' in code. That explains why nothing changed." Trial3 was effectively a repeat of the default coefficients, not a test of the candidate. **The a+A candidate is still unvalidated on real hardware** - re-run with the flag actually flipped and confirmed before it's flashed whenever this gets picked back up.

**2026-09-05, later same day: corrected re-run, `a+A` gdeq candidate CONFIRMED on real hardware for group delay.** `Corrected_gaA_Trial3_Optimised_gd_TF.txt`, flag verified active this time. p-p came back **87.7µs** (predicted 73.0µs) — RMS difference from the candidate's own predicted curve is just 2.7µs, versus RMS 50.1µs from the default coefficients' curve (Trial2, 157.5µs p-p) — a clean, correctly-shaped match this time, confirmed at the phase level too (up to ~65° of real differential phase vs. Trial2 near 4300Hz, vs. <2° in the first, misflashed attempt). A genuine ~1.8x real-hardware dispersion improvement over the default coefficients. **Scope caveat: this validates group delay, not IMD** — the earlier `aAgdeq_optimise_comparison.xlsx` "no measurable IMD difference" result (2026-09-04 entry above) predates this session's discovery that the flag can silently fail to take effect, and hasn't been checked for the same problem — treat that IMD finding as unconfirmed rather than settled until it's re-run with an explicit flag/build check. Chart `gdeq_trial3_candidate_check.html` updated and re-verified (light/dark, hover tooltip). Full numbers in `group_delay_fit_notes.md`'s matching entry.

**2026-09-05, later still: the a+A candidate needs a real, large `relative_delay` retune — 2 samples (125µs) → 4.5 samples (280µs) for minimum IMD, correcting the "no retune needed" prediction.** User's bench report: "The new gdeq makes quite big changes but one is the delay has to be set to 280us (4.5samps) to get near to minimum IMDs." That +2.5-sample (+156µs) shift is real and substantially bigger than a TFA-curve reading predicts: local delay at the actual 700/1900Hz tone frequencies is ~52-64µs (~1 sample) higher for the candidate than the default — right direction, well short of the size; local delay at the two-tone signal's own beat-frequency harmonics (1200/2400/3600/4800/6000/7200Hz — the frequencies the envelope/`freq_dev` signals actually contain for a two-tone pair) is an even worse predictor, flipping sign by the 4th harmonic and averaging near zero. Notably, this isn't a new gap: the default coefficients' own bench-optimal (125µs) already sat 71µs below their curve's full-band TFA mean (196.4µs) before this change. **Working explanation (unconfirmed): the `freq_dev` null-crossing transient being time-aligned is a broadband, non-stationary event, not a stationary tone — no single point on a linear TFA curve should be expected to predict its alignment optimum.** Bottom line: `relative_delay_samples` for the a+A candidate should be set from this bench value (4.5 samples), not derived from the TFA curve. Full breakdown and both prediction attempts in `group_delay_fit_notes.md`'s matching entry.

**2026-09-05, correction, shortly after: the 4.5-sample figure was a measurement artifact — `ENVELOPE_INTERP_FACTOR` (x4 interp) had been left on.** User: "realised that the x4interp had got switched on. With that off a more sensible 2.83 samples now." The broadband-transient explanation above is retracted — not needed. With the confound removed, the shift is 2 → 2.83 samples (+0.83 samples), which matches the local-TFA-delay-at-1900Hz prediction (+0.829 samples) almost exactly. The simple "read local group delay at the actual tone frequency" approach was right all along. **Use `relative_delay_samples` = 2.83 samples (~177µs) for an a+A-candidate preset, not 4.5.** Full recalculation in `group_delay_fit_notes.md`'s matching entry.

**2026-09-05, later still: full-spectrum comparison confirms a mixed result — close-in IMD better, far-out spur forest much worse.** `2gaA_spectra.txt` (default) vs `3gaA_spectra.txt` (a+A candidate), both real-hardware RF spectra, 700/1900Hz two-tone, `g`+`a`+`A`+`eq` all on. User: "the new gdeq fit gives a complex result. In general I would rate it worse even though the close-in IMDs are better. I see this pattern with the white noise into mic too although less obvious." **Both halves confirmed quantitatively.** Classic 3rd-11th order two-tone IMD products are all equal-or-better with the candidate (e.g. 3rd order: -34.2dBc → -41.5dBc low side, -29.8 → -35.7dBc high side). But a much larger, denser forest of additional intermodulation lines appears roughly 4-16kHz from the carrier on both sides, running **+11 to +19dB higher** with the candidate than the default in the same windows — not a raised broadband floor (the floor between lines is essentially unchanged), specifically many more discrete lines reaching much higher levels. These fall on the 100Hz grid set by GCD(700,1900)=100Hz — genuine intermodulation of the same two tones, consistent with the sharp `freq_dev` null-crossing nonlinearity generating a much richer comb than classic low-order IMD, not an unrelated spur. **Not recommending the a+A candidate for adoption on this evidence** — real close-in and group-delay improvements, but a real far-out spectral cost too. User confirmed both spectra were captured at each config's own optimised `relative_delay` (2 vs. 2.83 samples) — so the far-out forest is a genuine, apples-to-apples property of the candidate, not a delay-misalignment artifact. Chart `gaA_spectra_comparison.html`, full breakdown in `group_delay_fit_notes.md`'s matching entry.

**2026-09-05, later still: checked whether gdeq itself can be redesigned to fix this — no, not within this filter architecture.** User: "So can gdeq filter be improved? It obviously now has a big influence." Ran the same grid+polish optimization method already used for every fit in this project, against the identical analog+shelf1+shelf2 target curve, with several objectives beyond plain p-p: p-p+smoothness-penalty (2 sections, a1≠a2 allowed), pure RMS-slope minimization, and 3-/4-section cascades. **Every one of them converges back to essentially the same coefficients already in use (a≈-0.139), or trades a marginal 1-2µs p-p gain for a huge 60-125µs mean-delay increase.** The a+A candidate is already at (or extremely near) the Pareto-optimal point for both p-p dispersion and local smoothness simultaneously — there's no better coefficient set for a same-kind refit to find. Also found: the linear group-delay MODEL doesn't even predict the local steepness difference seen in the measured curves (2.8-4.5kHz) that's the likely proximate cause of the far-out spur growth — so whatever's really happening there is outside plain group-delay theory (a genuine hardware nonlinearity, or session-to-session measurement variability between the two separately-captured sweeps). **Redesigning gdeq is very unlikely to fix this — that avenue is now checked and effectively exhausted.** Recommended next step: a same-sitting, same-session A/B (flip only the flag, nothing else) to confirm the effect survives tighter control before investigating further. Full derivation, all coefficient-search results, and the smoothness-metrics table in `group_delay_fit_notes.md`'s matching entry.

**2026-09-05, later still: `'G'` added — live runtime toggle between the two gdeq coefficient sets, replacing the old compile-time-only `ENV_GDEQ_USE_AA_CANDIDATE` flag.** User: "Yes make it flipable between gdeq curves. g & G?" `'G'` (mirrors `'g'`/`'a'`/`'A'`) switches between the default pair and the a+A candidate live, re-initializing both allpass sections (no glitch, no reflash), and prints the 2026-09-05 mixed-result caveat plus the correct `relative_delay` retune numbers (2 vs. 2.83 samples) every time it's used. Only does anything on `ENV_FILTER_PNP_BC327_ATTN` @ 16000Hz (the only filter/Fs this candidate was ever fit for) — a plain "no effect on this build" reply otherwise. Persisted via a new trailing `PersistentSettings` field, `env_gdeq_use_aa_candidate`, off by default like every trailing field before it — this is a mechanism change (enables the same-sitting A/B this project's own last entry recommended), not a change in recommendation; the candidate stays off by default. Full implementation notes in `group_delay_fit_notes.md`'s matching entry.

**2026-09-05, later still: re-checked "already Pareto-optimal" and found a real, previously-missed p-p/local-smoothness trade-off — not about the low end specifically, but close to it.** User: "Doesn't the gdeq result suggest it's more important to fit the lower freq end more accurately?" **Turns out the candidate isn't uniquely more accurate at the low end — it's dramatically flatter at BOTH the low (100-3000Hz, p-p 57.1→17.5µs) and high (4500-8460Hz, p-p 124.9→73.0µs) ends, and pays for it by dumping curvature into the 2.8-4.5kHz transition in between (p-p there 20.1→53.6µs, worse) — the same zone already flagged as the site of the measured local-slope discrepancy.** That's a mechanical consequence of asking one smooth 2-section allpass to flatten the whole band: squeezing both plateaus flatter necessarily narrows and steepens the one transition it has to give. The earlier "no better coefficient exists" conclusion used RMS-slope averaged over the WHOLE 100-8000Hz band, which dilutes a spike confined to a ~1.7kHz-wide zone — re-running the search with the objective made LOCAL (minimize max slope specifically inside 2800-4500Hz, subject to a p-p budget) found real headroom: giving up ~20-25% of the candidate's own p-p advantage (still ~1.7-1.8x better than default) buys a 20-30% reduction in that local slope spike, robust across four different smoothing-window checks. Not dramatic, and the resulting coefficient pair isn't yet stable enough to bench-test (it moved around across smoothing windows, unlike the original candidate's robustly-converging fit) — would need a redo against the clean analytic target curve before it's a real third option. Full band-by-band breakdown and the Pareto-frontier numbers in `group_delay_fit_notes.md`'s matching entry.

**2026-09-05, later still: same-sitting `'G'` A/B confirms the far-out spur gap is real (not session-to-session variability); chirp sweep improved ahead of the next fit attempt.** User: "the g/G switch clearly confirms previous data. Nothing else in the way." — rules out the "maybe it was just measurement variability between sessions" possibility this file's design-search entry left open; the spur forest is a genuine coefficient-dependent effect. User then offered to improve the TFA sweep for a redo of the mid-band fit search: "Do you need smoother TF data? ... A slower sweep would be helpful and also do away with the zero wait, maybe sweep up and down rather than the big jump." **Yes — implemented all three in firmware:** `CHIRP_SWEEP_SEC` 5.0s → 15.0s (per leg); new `CHIRP_BIDIRECTIONAL` (default on) sweeps up then back down before muting/restarting instead of jumping straight back to the start frequency, giving two independent phase readings per cycle (a free up-vs-down consistency check on the measurement chain itself — correction to first-pass reasoning: the old jump was never an actual analog glitch, since it happened right as the envelope was about to mute anyway); `CHIRP_MUTE_SEC` left as-is (already tunable) since it's the sync marker the external rig triggers on and whether it's safe to shrink is a call for whoever runs that rig, not firmware. `'w'` status line and boot banner updated to report total sweep time and up+down vs. up-only. Next step once new data comes in: redo the mid-band Pareto search from the entry above against cleaner/full-resolution data. Full details in `group_delay_fit_notes.md`'s matching entry.

**2026-09-05, later still: added a linear sweep option (`CHIRP_SWEEP_LOG`), with a gotcha flagged before using it.** User: "is there a linear sweep option?" Added, default stays log (unchanged behavior). **Caution:** with the current 20Hz-20000Hz sweep endpoints unchanged, switching to linear actually gives WORSE resolution in the 100-8000Hz band this project cares about, not better — log already spends ~87% of its time below 8000Hz (most octaves in a 20-20000Hz log sweep fall below 8kHz), while linear over the same endpoints spends only ~40% of its time there. To get more resolution specifically in the mid-band (2.8-4.5kHz) for the gdeq refit, `CHIRP_F1_HZ` needs to come down (e.g. to 8000Hz) at the same time linear is turned on — flipping the law alone won't help. Full numbers in `group_delay_fit_notes.md`'s matching entry.

**2026-09-06: found and fixed a real bug — gdeq/ampeq in the `'w'` chirp path were running at 4x their design rate.** User: "I enabled the x4 interp in code (off for I cmd) to get higher Fs for w test but dont have the a+A in w test circuit?" **Root cause: `ssb_mic_test.ino`'s chirp branch was calling `envelope_gdeq_process()`/`envelope_ampeq_process()` on every fast tick (64kHz at stock `ENVELOPE_INTERP_FACTOR=4`) instead of only on full ticks (16kHz, `SAMPLE_RATE_HZ`) the way the normal production pipeline already correctly does.** Both filters' coefficients are fit assuming `SAMPLE_RATE_HZ` — running them 4x faster than designed collapses their effect on a 100-8000Hz sweep by roughly 17-58x (calculated: candidate p-p 70.9µs→4.1µs, default 69.3µs→1.2µs) rather than literally disconnecting them — exactly matching "don't have a+A in the w test circuit." **Fixed:** gdeq/ampeq (and the DC mapping after them) now run only on `is_full_tick`, handing off to the same `envelope_interp_on_full_tick()`/`on_interp_tick()` PWM-smoothing machinery the normal pipeline uses, instead of a direct PWM write every fast tick — so `'I'` now controls the `'w'` test's PWM smoothness independently of gdeq/ampeq's fixed processing rate, and raising `ENVELOPE_INTERP_FACTOR` for a smoother sweep no longer silently changes what gdeq/ampeq measure. **Open question, flagged not resolved:** this bug's mechanism has been in the code since gdeq was first wired into the chirp path (2026-09-02), yet Trial1/2/Corrected-Trial3 (all after that date) matched `Fs=16000`-based predictions closely — meaning either `ENVELOPE_INTERP_FACTOR` was smaller (plausibly 1) during those captures, or something else differed; not settled, doesn't block the fix. **Not yet bench-validated** — recommended check: re-run `'w'` with `'a'`/`'A'` and confirm their effect is visibly restored before trusting new sweeps for the mid-band gdeq refit. Full derivation in `group_delay_fit_notes.md`'s matching entry.

**2026-09-06, later same day: a+A confirmed restored; found a real bug in the fix itself; most likely explanation given for a new reported glitch.** User: "It could be that we only measured a+A at 16kFs but it was making the higher res capture more difficult." (agrees with the open question above) then "a+A in circuit now but the sweep wave form has discontinuities, looks likes it is skipping sample count?" **Found and fixed a genuine bug:** the fix's first draft captured `envelope_interp_on_full_tick()`'s `tick_start_us` AFTER running gdeq/ampeq/DC-mapping instead of at the true top of the tick — exactly the mistake `envelope_interp.h`'s own v4.1 note warns makes the PWM ramp's timing wrong. Fixed (moved the timestamp capture to before `test_signals_generate_chirp()`). **However this bug only matters with `'I'` ON** — checked `envelope_interp.cpp` directly: with `'I'` off, no ramp math runs at all, so it can't explain the reported symptom right now. **More likely explanation: this is the fix working as intended, not a new bug** — before the fix, chirp wrote PWM every fast tick (64kHz) regardless of `'I'`; after the fix, with `'I'` off, PWM only updates once per full tick (16kHz) — a real, intentional 4x coarser ZOH staircase (the same baseline behavior the normal pipeline has always had with `'I'` off), which would look exactly like new discontinuities on a scope. **Try `'I'` ON** — before the fix it had zero effect on chirp; now it should restore smooth interpolated PWM output for `'w'` too (one extra ~62.5us of delay, per `envelope_interp.h`'s documented tradeoff). If that doesn't resolve what's being seen, worth a precise description of exactly what's being viewed (scope on RSET/PWM? the reference pin? the captured TF data itself?). Full derivation in `group_delay_fit_notes.md`'s matching entry.

**2026-09-06, later same day: sharper explanation — gdeq/ampeq CANNOT be meaningfully characterized above 8000Hz, period.** User: "Are we kidding ourselves here? If the a+A filters are at 16kFs surely we are not going to get any sense out of them at 20kHz sine input?" **Correct — hard Nyquist limit, not a bug, no firmware fix changes this.** `SAMPLE_RATE_HZ`=16000 caps gdeq/ampeq's Nyquist at 8000Hz; above that, the chirp envelope genuinely aliases once correctly rate-matched (yesterday's fix), producing jagged, discontinuous-looking output — very likely the REAL, more complete explanation for the reported discontinuity (check whether it's confined to the upper ~13% of each sweep leg, above 8kHz, rather than the "'I' off ZOH" explanation given a moment before). **The `'w'` test has always had two conflated purposes:** characterizing the bare ANALOG filter alone (`'g'`/`'a'`/`'A'` off — continuous-time, genuinely valid to 20kHz, which is what `CHIRP_F1_HZ`=20000 is actually for) vs. characterizing gdeq/ampeq's own digital correction (`'g'`/`'a'`/`'A'` on — hard-capped at 8000Hz, same band every fit in this project has always used). **Recommendation: only trust `'w'` data below 8000Hz when `'g'`/`'a'`/`'A'` are on** — for gdeq-refit-focused sweeps, worth lowering `CHIRP_F1_HZ` to ~8000 to avoid wasting sweep time in a range that's now correctly aliased-and-useless rather than just quietly wrong; keep 20000 for bare-filter-only sweeps. Full derivation in `group_delay_fit_notes.md`'s matching entry.

**2026-09-06, later same day: a third, independent cause — going back to x1 interp for the 8kHz-capped sweep reintroduced a different Nyquist problem, this time in the raw chirp GENERATOR itself, not gdeq/ampeq.** User: "So I'm now back to x1 interp and 20-8k sweep. I now see a strange signal on the digital output - it is jumping in freq, like pwm changes... Worse at HF." **Root cause: `test_signals_generate_chirp()`'s own phase-accumulator sine has its own Nyquist limit, `fs_fast/2` where `fs_fast = SAMPLE_RATE_HZ * ENVELOPE_INTERP_FACTOR`** — unrelated to gdeq/ampeq's 8kHz digital-EQ ceiling or the analog filter's 20kHz ceiling (the two Nyquist limits already documented above). At `ENVELOPE_INTERP_FACTOR=1`, `fs_fast=16000Hz`, so the sweep's new 8000Hz top sits EXACTLY on that limit — only 2 samples/cycle, zero margin, a degenerate case where any tiny sweep-to-sweep frequency change swings the apparent instantaneous frequency wildly. **Verified by simulation:** at factor=1 the generated waveform's zero-crossing-based frequency estimate near the sweep top has std-dev 1257Hz (swinging 4000-8000Hz); at factor=4 (`fs_fast=64000Hz`, 8 samples/cycle, 75% Nyquist margin) it's 480Hz — matches "jumping in freq... worse at HF" closely. **Fix: no longer a trade-off** — the 2026-09-06 `is_full_tick` fix (two entries above) already means gdeq/ampeq see genuine `SAMPLE_RATE_HZ`-rate samples regardless of `ENVELOPE_INTERP_FACTOR`, so raising it back up only helps the raw generator with zero downside for gdeq/ampeq correctness. **Recommendation: `ENVELOPE_INTERP_FACTOR=4` (stock value, `envelope_interp.h`) + `CHIRP_F1_HZ=8000` (already updated in `config.h`) together** — resolves both this symptom and the earlier above-8kHz aliasing concern at once; no other firmware change needed, this is a config-value revert on the user's own local `ENVELOPE_INTERP_FACTOR` edit. Secondary, likely-minor compounding factor also present at factor=1: `resolution_hz = 2000000UL * ENVELOPE_INTERP_FACTOR` (PWM timer resolution) is 4x coarser too. Full derivation and the samples-per-cycle table in `group_delay_fit_notes.md`'s matching entry.

**2026-09-06, later still: `HiRes_aA_TF.txt` - the sweep fix worked (clean, no in-band jitter), but the curve's SHAPE matches the DEFAULT gdeq coefficients, not the a+A candidate.** User: "OK how does this look?" (first sweep after the `ENVELOPE_INTERP_FACTOR=4`/`CHIRP_F1_HZ=8000` fix above). **Good news:** 1365 points at ~5.86Hz spacing, 4x finer than any prior sweep - checked sample-by-sample through 35-7900Hz, smooth and monotonic, zero jitter/discontinuities, confirming the fix. Only rough spots are the classic TF-rig edge artifacts already seen on every past sweep (near-DC leakage below ~35Hz, reduced SNR right at the sweep's own top above ~7900Hz) - not new, not related to the chirp-generator Nyquist fix. **Real finding: comparing the smoothed group-delay shape (mean-removed) against the two known real-hardware reference curves shows a correlation of 0.877 with `ga_Trial2_TF.txt` (default gdeq coefficients, a+A on) vs. only 0.463 with `Corrected_gaA_Trial3_Optimised_gd_TF.txt` (the confirmed-good a+A candidate capture)** - RMS 22.8us vs. 34.0us respectively. **Likely explanation: `'G'` probably wasn't actually switched to the candidate for this capture** - the same class of mistake as the 2026-09-05 "Flag wasnt '1' in code" incident, now via the runtime `'G'` toggle instead of the old compile-time flag. Recommend checking `'G'`'s state (the `'w'` reply already prints it) and recapturing before using this sweep for the mid-band refit. Comparison chart (mean-removed, hover tooltip): `hires_aA_shape_check.html`. Full numbers and the Savitzky-Golay edge-contamination gotcha (smooth-then-trim, not trim-then-smooth-across-the-whole-array) in `group_delay_fit_notes.md`'s matching entry.

**2026-09-06, correction, shortly after: `HiRes_aA_TF.txt` had `'g'` OFF entirely (only `'a'`+`'A'` on) - re-checked against the right reference and it's a clean, good match.** User: "the sweep was only a+A enabled." Retracting the "'G' probably wasn't toggled to the candidate" read above - the right comparison target for a gdeq-off sweep is the **bare** analog+shelf1+shelf2 curve (no gdeq processing at all), not either gdeq-corrected reference. Re-ran the shape check against `gdeq_refit_a_A_candidate_v7.html`'s own `DATA.bare` array: **correlation 0.940, RMS 12.3us** (mean-removed) - a genuinely good match, robust across smoothing-window choice (0.90-0.95 across 350-900Hz windows). This also explains the earlier (mistaken) lean toward "looks like default gdeq": the bare curve happens to correlate reasonably with the default-gdeq-corrected curve too (0.925) because the default coefficients were fit before the a+A shelves existed and don't meaningfully flatten the combined curve any more (they even push its own p-p UP, 88.8us bare -> 157.9us with default gdeq added) - while the candidate coefficients, fit specifically against bare+shelf1+shelf2, do flatten it (down to 73.0us) and so correlate poorly with the un-equalized bare shape (0.506). **Bottom line: this sweep is exactly what it should be - a clean, high-resolution, directly-measured version of the bare target curve**, with none of the smoothing-window sensitivity that made the earlier reconstructed-from-Trial2-data version unstable (2026-09-05 "already Pareto-optimal" entry). This is the real, clean input the parked mid-band local-slope-vs-p-p Pareto search needed - not yet re-run (still parked pending go-ahead), but the data-quality blocker that search was waiting on looks solved now. Updated chart (now plotted against the correct bare-curve reference): `hires_aA_shape_check.html`. Full derivation in `group_delay_fit_notes.md`'s matching entry.

**2026-09-06, later still: `HiRes_aAg2_TF.txt` and `HiRes_aAG3_TF.txt` cross-checked before use - both confirmed correctly configured, three ways.** User: "Should cross check these first?" - yes, given the mixup two entries above. Same pipeline, same clean-between-edges character as every HiRes sweep. Results: `HiRes_aAg2_TF.txt` matches the **default gdeq** prediction (corr 0.972, RMS 12.7us) and clearly not the candidate (corr 0.005); `HiRes_aAG3_TF.txt` matches the **a+A candidate gdeq** prediction (corr 0.903, RMS 12.5us) and clearly not default (corr 0.208) - both land on their filename's claimed config. **Strongest check: subtracting the two measured curves cancels the shared analog+shelf path algebraically, leaving just the pure candidate-minus-default gdeq difference** - measured vs. analytic model: correlation 0.997, RMS 3.2us. This validates both sweeps at once and doesn't depend on the bare-curve reconstruction being accurate at all. **All three HiRes sweeps (bare, default-gdeq, candidate-gdeq) are now confirmed clean and correctly configured** - the best, most self-consistent real-hardware dataset this project has had, ready to use for the mid-band Pareto refit search. Three-way chart: `hires_three_way_validation.html`. Full numbers in `group_delay_fit_notes.md`'s matching entry.

**2026-09-06, later still: mid-band local-slope-vs-p-p Pareto refit redone against the real HiRes data ("yes go ahead").** Fixed a units bug first (naive `np.diff` on an interpolated group-delay curve gave absurd 700+us/kHz slopes - fixed by a second, wider-window `deriv=2` Savitzky-Golay pass on the raw unwrapped phase instead, window 2500Hz), then validated the additive bare+gdeq model against the real measured `aAG3`/`aAg2` sweeps (corr 0.970/0.982, RMS 7.1/8.6us) before trusting it. Full grid search over `(a1,a2)`, objective = worst-case 2800-4500Hz local slope subject to a full-band p-p budget: **the current a+A candidate (`a1=a2=-0.139115`) makes local mid-band slope WORSE than doing nothing** - 49.6us/kHz vs. bare's own 36.8us/kHz, at p-p=105.2us - the sharpest, now real-data-confirmed version of the far-out two-tone spur-forest explanation flagged 2026-09-05. Proposed **candidate B, `a1=a2=+0.09`** (p-p=136.0us, local slope=28.1us/kHz - a 43% reduction vs. the current candidate, within 2us/kHz of default's own best-achievable slope while staying meaningfully better than default on p-p). Drops into the existing `ENV_GDEQ_A1_AA_CANDIDATE`/`A2_AA_CANDIDATE` slot with no architecture change, but is a MODEL prediction only - not yet bench-tested, needs a real TFA sweep and two-tone spur-forest A/B before being trusted. Open question raised but not yet resolved: whether to bench-test it via a quick macro-value swap (loses the ability to instantly A/B back to the current candidate) or a proper three-way `'G'`-cycling extension (bigger change, no compiler available to catch mistakes). Chart: `mid_band_pareto_refit.html`. Full derivation in `group_delay_fit_notes.md`'s matching entry.

**2026-09-06, later still: candidate B wired into `'G'` as a proper third option ("in my list it will be G#4").** Went with the more invasive of the two options above rather than a macro-value swap, so all three sets stay live-A/B-able. `'G'` now CYCLES default -> a+A candidate -> candidate B -> default -> ... (same modulo-cycle convention as `'C'`/`'f'`), skipping any set the active build never fitted. New `env_gdeq_variant_t` enum replaces the old boolean across `envelope_gdeq.h`/`.cpp`, `serial_commands.cpp` ('G' handler, 'w' status line, 'P' settings dump), `settings.h` (the trailing `PersistentSettings` field migrated bool->enum in place - safe because none of the 10 presets ever set it explicitly, all rely on zero-fill, same kind of migration `adc_lpf_mode` went through for the Chebyshev filter option), and `ssb_mic_test.ino`'s boot banner. No compiler available, so verified by grepping the whole project for every reference to the retired API and confirming only dated historical prose survives, plus checking the new cycle logic against the two existing modulo-cycle precedents. Candidate B is STILL not bench-tested - this only makes it selectable; the real TFA sweep + spur-forest A/B the entry above called for hasn't happened. Full derivation in `group_delay_fit_notes.md`'s matching entry.

**2026-09-06, later still: candidate B bench-tested (`HiRes_aAG4_TF.txt`) - group delay confirmed and even better than modeled, but real IMD gain small and delay-sensitive.** Cross-checked correctly configured (corr 0.995/RMS 3.8us vs. the candidate-B prediction, clearly not the a+A candidate at corr 0.388). Measured 2800-4500Hz local slope came back at 22.6us/kHz - BETTER than the 28.1us/kHz predicted, and better than even default's own modeled best-achievable 26.1us/kHz. **But real bench IMD result (user, verbatim): "Change in IMDs very small - maybe highest IMDs see an improvement but it depends on 1 or 2 steps on delay setting to choose a compromise in either the default or new candidate, ie 2.00 to 2.10. In compromise I mean some go up and some go down in either candidate."** Group-delay theory has now been validated to a few us RMS against real hardware three times over (aAg2/aAG3/aAG4) but has twice now failed to reliably predict real two-tone IMD improvement (same lesson as the 2026-09-05 spur-forest finding) - candidate B is a real, working design but not a clear net upgrade over default or the original a+A candidate. No default change recommended from this result. Chart: `hires_candidateB_validation.html`. Full derivation in `group_delay_fit_notes.md`'s matching entry.

**2026-09-06, later still: `'G'`'s candidate-B reply was overflowing `serial_reply()`'s 512-byte buffer and getting silently-then-visibly truncated - fixed by shortening + updating the stale caveat text.** User: "the 'B' message is too long and is truncated with warning." Root cause: the candidate-B variant name plus its `'G'`-handler caveat string totaled 607 bytes against a 512-byte buffer - and the caveat text was itself already stale (still said "MODEL PREDICTION ONLY, NOT YET BENCH-VALIDATED... no bench optimum found yet" from before the `HiRes_aAG4_TF.txt` bench test above landed). Fixed both at once: `envelope_gdeq_variant_name()`'s candidate-B string dropped its baked-in status clause (now just `"candidate B (a1=a2=+0.09)"`, matching the other two variants' style - status text lives only in the `'G'` handler's caveat and the header comment now, so it's updated in one place); the caveat itself rewritten to report the actual bench result (group delay confirmed, real IMD gain small and delay-tuning-dependent within the 2.00-2.10 sample compromise window) at 418 bytes total, comfortably under budget. Also updated the `ssb_mic_test.ino` boot banner's matching `'G'` description (same stale wording, not the actual overflow bug since `Serial.printf()` doesn't route through `serial_reply()`'s guard, but worth fixing together) and appended an "UPDATE, 2026-09-06" pointer note to `envelope_gdeq.h`'s header comment (old "not yet bench-validated" prose kept, not deleted, per this project's correction convention). Full derivation in `group_delay_fit_notes.md`'s matching entry.

**2026-09-07: the long-standing "top-priority" AM-to-PM crosstalk asymmetry item (`'h'`/AMTEST, -2.4kHz nulls / +2.4kHz stuck at -40dB) is NOT currently reproducible - `'h'` now behaves like textbook symmetric AM.** User, re-running `'h'`: "AM test now seems well behaved and symmetric IMDs go up and down with modulation level in a sensible looking way, only getting large (-40) towards 100% modulation" - exactly the expected pattern for a clean AM path (distortion scaling with modulation depth, worst near full-scale envelope swing), not the old asymmetric signature. Neither this file, `group_delay_fit_notes.md`, nor `null_bias_investigation.md` had a dated record of the original asymmetry ever being measured or of a completed `'g'` on/off comparison against it - the "top-priority" language was a years-old undated header note in `envelope_gdeq.h`, not a logged result. Whether this cleared as a side effect of an unrelated fix (the GPIO13/GPIO39 ADC-coupling fixes are the right general class) or was never as reproducible as the note suggested is unknown either way. Matters beyond just closing an old item: this is evidence AGAINST AM-to-PM crosstalk as the explanation for why flattening group delay hasn't produced a clean two-tone IMD win (the 2026-09-05/09-06 spur-forest and candidate-B writeups both named it as a leading suspect) - narrows that mystery toward envelope-domain nonlinearity or a mechanism not yet identified. Full derivation in `group_delay_fit_notes.md`'s matching entry.

**2026-09-07, later same day: closing the loop - `'g'` on/off on `'h'` makes almost no difference.** User: "g makes almost no difference - a dB or two on high order imd that are already at -60dB, some up, some down." Completes the `'g'` on/off comparison flagged as the one remaining piece above - a 1-2dB, both-directions change on IMD products already at -60dB reads as measurement scatter, not a systematic effect. Expected result: gdeq is a unity-magnitude allpass that can only shift the timing of the AMTEST envelope's own harmonic content, not create/remove/reshape it in amplitude, and (per the entry above) there's no longer an asymmetry present for it to influence either way. AM-to-PM crosstalk is now doubly ruled out via `'h'` specifically - envelope-domain nonlinearity or an unidentified mechanism remain the live candidates for the still-open two-tone spur-forest question. Full derivation in `group_delay_fit_notes.md`'s matching entry.

**2026-09-07, later still: `'E'` added so the existing duty-override stepper can validate `'D'` (predistort) - plain `'d'` mode structurally couldn't.** User: "check the distortion correction... put in loop (on/off) for the duty measurement ('d')" - has an automated harness that already drives `'>'`/`'<'`/`'N'`/`'B'` and planned to just run it twice (`'D'` on, `'D'` off). Caught first: `'d'` direct duty override bypasses the entire envelope pipeline by design (it's literally how the predistort LUT's own raw characterization data was gathered), so `'D'` can never show a difference there - that plan would have produced a misleading null result. `'E'` toggles what the SAME stepped value means: off (default) it's still the raw duty count; on, it's a commanded envelope run through `'D'`'s predistort LUT (or the linear mapping if `'D'`'s off) before being converted to duty - same stepper, same keys, so the existing automated harness needs no changes beyond sending `'E'` once. See the Direct Duty Override section above for the exact reply format. Full derivation in `group_delay_fit_notes.md`'s matching entry.

**2026-09-07, later still: first real-hardware run of the new `'E'` sweep (`DistortionLinearisationEcmd.txt`, 1024 points, `'D'` ON throughout) - confirms predistort genuinely linearizes envelope->RF-amplitude on real hardware, to well under 0.5dB.** Clean capture, no drops, `'E'`'s reply format round-tripped through the user's automated logger exactly as intended. Fit `dBm = A + 20*log10(frac)` (ideal proportional-amplitude model): RMS residual 0.134dB (frac 0.10-1.0) / 0.144dB (frac 0.02-1.0), max |residual| 0.545dB, no systematic bow across the range - measurement-noise-level ripple, not leftover nonlinearity. Sharp growth below frac~0.02 is the expected turn-on-knee/noise-floor edge, not a defect. **First real-hardware confirmation predistort's LUT actually linearizes end to end** (not just self-consistent against its own fitting data) - closes the static-linearization open item, but does NOT touch real IMD (a separate, still-open question). Only the `'D'`-ON half of the originally-planned two-pass comparison exists so far; the `'D'`-OFF companion run is still pending. Chart (measured-vs-ideal overlay, a lin/lin panel added same day per request, and the residual trace): `predistort_e_validation.html`. Full derivation in `group_delay_fit_notes.md`'s matching entry.

**2026-09-07, later still: the `'D'`-OFF companion run (`DistortionLinearisationEcmdDoff028054.txt`, offset=28%/scale=54% per the data-driven window from the REVISION 6 duty sweep) completes the two-pass comparison - the LUT wins almost entirely on reaching a genuine off-state, not on in-range linearity.** Clean 1024-point capture, same as the `'D'`-on run. Floor at frac=0: -59.08dBm (linear mapping) vs. -98.43dBm (LUT) - a **39.4dB worse off-state**, structural and unavoidable for ANY single linear offset/scale choice that also needs to stay linear across its working range (the real duty->amplitude curve's dead zone sits outside that straight-line region by construction). But IN-range, fitting the physically correct affine model (amplitude = m*frac + c) gives 0.210dB RMS / 1.34dB max across the full range - only modestly worse than the LUT's 0.144dB RMS / 0.545dB max, and restricting to frac 0.5-1.0 with the same proportional model used for the LUT gives 0.213dB RMS / 0.489dB max, essentially matching it. Also validates the earlier window-picking method itself: predicted ~0.7%/~2% amplitude-normalized error, measured 0.744%/2.650% - close agreement on real hardware, not just a lucky guess. Chart (dBm overlay of both runs, plus the D-off affine-fit residual): `predistort_doff_vs_don.html`. Full derivation in `group_delay_fit_notes.md`'s matching entry.

## Is `eq`'s IMD benefit the highpass or the presence boost?

**2026-09-03.** Real-hardware report: `eq` (`e`) still has the single biggest positive effect on IMD of anything tried — cleans up inter-modulation broadly, not just sideband symmetry the way `a` does. Can't currently be isolated: `eq_enable` gates the 300Hz HPF and the 2200Hz/+4dB presence peak together as one flag (`ssb_dsp.c`). Working hypothesis (not yet confirmed): more likely the HPF — this project's own null-crossing bias investigation (`null_bias_investigation.md`, 2026-08-31) already treats `e` as a confound for two-tone null-crossing phase behavior (its reproduction workflow calls for `e` off before taking a clean reading), and a highpass removing near-DC content before the Hilbert transform is a plausible general mechanism for that; the presence peak has no obvious general-IMD mechanism at the 500–1900Hz two-tone frequencies in use. Splitting `eq_enable` into two independently-toggleable flags would let this be tested directly on the bench — not yet done, parked pending go-ahead. Full writeup in `group_delay_fit_notes.md`.

**Also 2026-09-03: why `eq` can't just be dropped into the `w` chirp/TFA loop the way `g`/`a` were.** `g`/`a` both run on an already-extracted envelope value, the same domain the chirp test directly synthesizes — a one-line addition each. `eq` runs upstream of the Hilbert transform, on the raw audio sample that determines BOTH the envelope and phase channels together — a domain the chirp fast-path bypasses entirely by design (it skips `ssb_dsp_process_sample()` altogether). Characterizing `eq` via TFA would need a real new test mode (a swept sine fed as the audio sample, `eq` on/off, capturing both resulting envelope and freq_dev_hz channels) — a legitimately new signal generator, not a one-line hookup. Not yet built. Full writeup in `group_delay_fit_notes.md`.

**2026-09-03, later same day: what is `eq` mechanistically doing to the 700/1900Hz two-tone signal?** Computed the exact RBJ response of both stages at the default two-tone pair. Ruled out tone-balancing: the HPF barely touches either tone (-0.14dB/-0.00dB), and the presence peak actually skews them further apart (+0.40dB@700Hz vs +3.60dB@1900Hz) rather than leveling them. Found instead: the HPF imposes a real **~126µs differential phase-delay between f1 and f2** (-144µs @700Hz vs. -18µs @1900Hz) even where their amplitude is untouched. Separately (relevant only if the two-tone signal is ever run through the real mic/ADC path instead of the synthetic generator): the 65-tap Hilbert FIR's own response rolls off hard below 300Hz and is mathematically null at DC. Full derivation in `group_delay_fit_notes.md`.

**2026-09-03, confirmed synthetic-only, and the leading mechanism found.** User confirmed all `eq` testing uses only the `'t'` synthetic generator — this rules out the Hilbert-FIR/real-world-hum mechanism above entirely (a pure two-tone sum has nothing sub-300Hz for the HPF to remove). Re-checked the COMBINED (HPF+presence, cascaded) response instead of each stage alone: **the presence peak's asymmetric boost (+3.34dB net between the two tones, almost entirely from presence, not the HPF) keeps the two-tone envelope from ever reaching a true zero null.** With `eq` off, equal-amplitude tones (`TWOTONE_AMPLITUDE`) null to an exact 0 at every destructive-interference point — the single hardest condition this project has already spent real effort on (the predistort LUT's steepest region, the `null_bias` discrete artifact, and the whole reason `'x'`/`'z'` exist all sit right at this exact condition). With `eq` on, the math works out to an envelope floor of **-14.4dB relative to peak instead of literal zero** — `eq` is likely doing, as an unplanned side effect of the presence peak, something close to what the dedicated `'x'` floor control does on purpose. This flips the earlier HPF-favoring guess: presence now looks like the more likely dominant contributor, not the HPF. **Falsifiable prediction:** presence-only should reproduce most of the benefit in a split-toggle test; HPF-only should show much less. Also worth trying directly, no code changes needed: dial in `'x'` with `eq` off entirely and see if it reproduces a similar benefit. Full numbers in `group_delay_fit_notes.md`.

**2026-09-03, resolved: `'x'` makes IMDs worse, and it's amplitude not tone-timing.** User confirmed `'x'` makes IMDs worse in every case tried, and — unprompted — confirmed the predicted envelope-floor behavior directly on the scope: `eq` off shows real zeros with bad (especially low-order inter-IMD) products; `eq` on shows a visibly higher floor and cleaner IMDs. Asked whether it's the tones' relative phase or amplitude. **Answer: amplitude.** Modeling the exact analytic signal shows why: with equal-amplitude tones (`eq` off), the sample nearest a null shows `freq_dev` flip sign entirely (+1300Hz either side, -6700Hz right at the null) — a genuine derivative discontinuity from the trajectory passing through the origin. With `eq`'s amplitude mismatch, `freq_dev` rises and falls smoothly through the same region with no sign flip. The envelope minimum (`|A1-A2|`) depends only on amplitude — a pure tone-timing shift only moves *when* a null occurs, never *whether* it's a true zero, so timing can't be what's fixing this. This also explains why `'x'` backfires: `envelope_floor_apply()` never touches `freq_dev_hz` (confirmed both in its own doc comment and the REVISION-1 postmortem above), so the real tones are still equal-amplitude, the violent `freq_dev` transient still happens — `'x'` just forces the reported envelope up at that exact instant, so the transient now rides a higher-amplitude carrier instead of a naturally near-zero one that would otherwise mask its spectral spread. `eq` fixes the actual root cause upstream; `'x'` papers over the symptom downstream without touching the cause. Full derivation in `group_delay_fit_notes.md`.

**2026-09-03, later same day: this is a known, named EER/polar-transmitter problem — researched, not guessed.** User asked whether this is something the field has tackled, and whether noise injection could help. Web-researched (not recalled from memory): yes, this is THE well-known Achilles-heel of Kahn-technique/EER/polar transmitters, traced to Kahn's original 1952 EER paper and precisely characterized in Zhuang, Waheed & Staszewski, IEEE Trans. Circuits and Systems I, 2010 ("~10× bandwidth expansion, or theoretically infinite... when the signal trajectory passes through or near the constellation origin" — matches this session's own `freq_dev` sign-flip finding closely). **The established fix matches what `eq` stumbled into**: alter the complex trajectory before the envelope/phase split, not the scalar envelope after — independently validating this session's finding that `'x'` (a post-hoc scalar remap) can't work while `eq` (an upstream amplitude change) can. Important nuance found in the same research: naive magnitude-only clamping (preserve angle, just push `|z|` up) provably does nothing to the angle/`freq_dev` trajectory — exactly why `'x'` failed. Phase-path bandwidth/slew-rate limiting is the other standard mitigation class — already built here (`freq_dev` slew limiter), not yet tested against this specific spike. **Dithering/noise injection: no precedent found for this specific problem** — plausible by analogy to general DSP dithering practice, but should be treated as an untested idea, not a known fix. Also retracting an earlier guess that the exact-zero null might be mainly a two-tone-test artifact — the cited paper reproduces the same effect with a real WCDMA signal, so it's a genuine concern. Full writeup and next-step ordering in `group_delay_fit_notes.md`.

**2026-09-04: closing out the ampeq/gdeq optimization line — envelope accuracy in both time (group delay) and shape (amplitude/magnitude) matters, and this hardware has been pushed about as far as is practical.** User's own summary after the a+A gdeq candidate's IMD-indistinguishable bench result (see `group_delay_fit_notes.md`): between the `a`/`A` ampeq shelf work (envelope amplitude accuracy vs. frequency), the `g` gdeq work (envelope group-delay accuracy vs. frequency), and now the a+A-specific gdeq refit, this project has repeatedly found real, measurable, directionally-correct improvements in the underlying physical quantity (magnitude flatness, delay flatness) that mostly fail to translate into a clear IMD win once actually bench-tested — and where they do help (shelf1 alone, some `A` cases), the gains are modest next to the dominant mechanism this project found instead: the amplitude-mismatch/true-zero-null effect documented throughout this section. Treating this line of investigation (further ampeq/gdeq refinement specifically) as having reached practical diminishing returns on this hardware setup, rather than continuing to chase smaller p-p/dB numbers that this project's own data says don't reliably predict IMD outcome.

**2026-09-04, same day: a direct, controlled test of the amplitude-mismatch hypothesis, sidestepping `eq` entirely.** User's proposal: since the leading mechanism (2026-09-03, resolved above) is specifically that unequal tone amplitude keeps the envelope off a true zero, the cleanest test is to generate the two tones at different levels directly, independent of `eq`'s HPF/presence coupling — if the hypothesis holds, this should reproduce (or exceed) `eq`'s IMD benefit with `eq` fully OFF, isolating the effect completely from the HPF and its own separate ~126µs differential-delay side effect (2026-09-03 entry above). Implemented as a new `'R'` key (`test_signals.cpp`/`.h`): cycles `TONE_RATIO_PRESETS` — equal (0dB, unchanged default), -1dB, -3dB, -6dB, -10dB, -20dB — scaling tone2 down relative to a fixed tone1, so the combined peak never exceeds today's level at any step (no new clipping risk). The -3dB step is deliberately placed to bracket the ~3.34dB net mismatch `eq`'s presence peak was measured imposing on the same 700/1900Hz pair, so a direct `'R'` @ -3dB (eq off) vs. `eq` on comparison at the same pair is the most direct falsification test available. Independent of `e`/`a`/`A`/`g` — combine or compare freely. Not yet bench-tested.

**If confirmed, the practical payoff the user flagged: an easy, principled fix for a "Tune" test signal.** A conventional two-tone tune/test signal uses equal-amplitude tones specifically because that's the traditional definition — but per this project's own findings, equal amplitude is exactly the condition that maximizes `freq_dev`'s null-crossing sign-flip and the resulting spectral splatter, on THIS architecture (polar/EER). If a modest, fixed amplitude offset between the two tones (no HPF, no presence peak, no dependence on `eq`'s other side effects) reliably cleans up the IMD/splatter the way `eq` does, that points to a simple, permanent design choice for any built-in tune/test signal on this hardware: generate it with a small deliberate tone-amplitude mismatch (e.g. the -3dB point, or whatever level bench-testing shows is the minimum needed) rather than the conventional equal-level convention, entirely sidestepping the EER null problem for that specific signal by construction — no DSP correction (`g`/`a`/`A`/`eq`) required for it at all. Doesn't address the general case (real speech/music routinely passes near the origin too, per the Zhuang/Waheed/Staszewski citation above) but would be a clean, low-risk win for the tune-signal use case specifically. Workflow to test: `T` to select a pair, `e`/`c`/`g`/`a`/`A` all off, `R` to step through ratios, compare IMD at each step against the `eq`-on baseline for the same pair.

**2026-09-04, later same day: hypothesis CONFIRMED on the bench — `'R'`'s amplitude mismatch alone, with `eq` off, reproduces the benefit.** User's own read: "that proves the hypothesis." One nuance surfaced in getting there: `eq`'s own mismatch actually makes tone2 (the upper tone, 1900Hz) *louder* than tone1, not quieter — the opposite direction from `'R'`'s original down-only implementation, which could only test tone2-quieter. Doesn't change the underlying conclusion (the mechanism depends only on `|A1-A2|`, not on which tone is bigger — see the 2026-09-03 derivation above), but means the original `'R'` range wasn't actually exercising `eq`'s own direction. **`'R'` reworked accordingly:** range narrowed from the original 0 to -20dB (tone2-down-only) to a symmetric `-3dB` to `+3dB` in seven 1dB steps (`-3,-2,-1,0(equal),+1,+2,+3`) — the wider steps (-6/-10/-20dB) are dropped since +/-3dB already confirms and exercises the effect; positive steps now let `'R'` match `eq`'s own boost-tone2 direction exactly. To make the boost direction safe and to keep every step directly comparable in overall drive level, tone1+tone2's combined constructive-interference peak is now held CONSTANT (0.9, unchanged from today) across every ratio — the split between the two tones changes, not the total. Full implementation notes in `group_delay_fit_notes.md`'s matching entry.

**2026-09-04, later still: reworked `'R'` re-flashed and re-tested in `eq`'s own (boost-tone2) direction — IMD came back very close to `eq` on. User's own words: "that answers that one."** Question closed: `eq`'s IMD benefit on the two-tone test is fully explained by the amplitude mismatch its presence peak incidentally introduces — a plain amplitude offset via `'R'` alone, no HPF, no presence-peak shaping, no other `eq` side effect, gets essentially the same result. Doesn't change anything about `eq`'s value for real mic content (HPF hum rejection, presence-peak tonal shaping still matter there for unrelated reasons) — this specifically closes why `eq` was the biggest IMD win on the synthetic two-tone test. Still open: the minimum mismatch actually needed (this used +3dB — whether +1/+2dB gets most of the way there hasn't been checked) and whether it holds at other `T` band pairs, not just 700/1900Hz. Full writeup in `group_delay_fit_notes.md`'s matching entry.

**2026-09-04, later still: working judgment — 1dB is enough for practical (tune-signal) use; comparative tests planned for completeness.** User's read on the "minimum mismatch needed" open question above: 1dB is expected to be sufficient for a practical tune-signal offset, well under the +3dB used to confirm the mechanism. Not yet bench-verified — comparative testing across the `'R'` range (and likely other `T` band pairs, the other still-open item above) planned for the next session. No code change from this — `'R'`'s existing 1dB-step range already covers the +1dB point directly.

**2026-09-05: the promised `'R'` sweep confirms the mechanism again at 700/1900Hz — and separately, the 300/500Hz pair has a real, unrelated USB→LSB image leak.** `Tone_Level_Ratio_IMDs.xlsx` — 3rd-order IMD at every `'R'` step moves in the expected direction each side of equal, consistent with everything above, no surprises there. **User's own side question, confirmed: yes, it's a USB leak to LSB.** Traced to the 65-tap Hilbert FIR's own magnitude response, not anything `'R'`/`eq`/`a`/`A`/`g` do: the I (direct) branch is a pure sample delay (exactly flat, always), but the Q (Hilbert) branch's magnitude only becomes accurate (>50dB image rejection) from ~500Hz up — at 300Hz specifically it's -0.72dB low, which works out to only ~28dB of opposite-sideband rejection (vs. 55-67dB everywhere else this project tests), enough to show up as a distinctly "high" spur. Phase is exactly -90° at every frequency by construction (a property of this Type-III FIR design), so this is a pure amplitude-imbalance effect, nothing to do with tone timing or delay. Structural, not a bug introduced by any recent work — would affect any real content near 300Hz and below the same way. Full derivation, the frequency table, and the options (accept it, lengthen `HILBERT_TAPS`, or just avoid placing a reference tone at/below 300Hz) in `group_delay_fit_notes.md`'s matching entry.

## Envelope pre-distortion

| Key | Effect |
|---|---|
| `D` | Toggle a measured-curve lookup table (`envelope_predistort.h`) that corrects the *static* (memoryless) nonlinearity of the whole envelope→RF-amplitude chain — PWM/RC filter, BS170 gate transfer curve, and the AD9851's own RSET-to-DAC-current relationship, all as one measured end-to-end curve. A different problem from `g`'s: this is amplitude vs. commanded level (present even with a constant carrier), not delay vs. frequency. **REPLACES** the `u`/`j`/`i`/`k` linear offset/scale mapping while on — those knobs have no effect until `D` is toggled off again. **Not yet validated beyond the measurement itself.** |

Off by default. Table derived from a real hardware sweep (single-tone + master-gain steps, PWM ranging at full 0–100% span) — see `envelope_predistort.h` for the full derivation and the curve's shape. Now on its 3rd revision: same 65 points, but rebuilt from an automated SDRuno logger's 363 dBm readings (0.1dB steps, -30dB to +6.2dB — far denser and wider-range than REVISION 2's 95 manually-read points over -16dB to +5dB), with the original 17-point gate-voltage curve kept but extended and spot-checked rather than re-measured (the automated logger can't read gate voltage — see `envelope_predistort.h`). Dead below ~30% duty, steep turn-on through ~30-55% (now wanting ~1-1.5 duty points more than REVISION 2 through most of that climb), then a gently-compressing climb that doesn't actually finish plateauing until ~98.8% duty, not the ~95% REVISION 2 assumed — REVISION 2's own top-end sweep wasn't quite dense enough to catch the last of it either, same class of miss as REVISION 1 had with the same region.

## Direct duty override

| Key | Effect |
|---|---|
| `d` | Toggle direct duty override on/off. While on, `dsp_task`'s normal envelope pipeline is locked out of the RSET output entirely — the LEDC hardware just holds whatever duty was last explicitly set below. Turning it on resets the working value to 0. Turning it off hands control back to the normal pipeline. |
| `>` | Step the working value +1 (finest resolution — only does anything while `d` is on) |
| `<` | Step the working value -1 |
| `N` | Step the working value +16 (coarse) |
| `B` | Step the working value -16 (coarse) |
| `E` | Toggle what the stepped value above actually means — see below. Works whether `d` is on or off. |

**Two modes, toggled by `E` (added 2026-09-07):** by default (`E` off, original behavior) the stepped value IS the raw duty count, bypassing master gain, envelope, `u`/`j`/`i`/`k` offset/scale, AND `D`'s predistort LUT entirely — each step prints `-> duty NNN/1023 (raw)`. This is what's needed for characterizing the RSET/PWM/filter/AD9851 chain against a *known, exact* commanded duty count instead of one inferred from a gate-voltage reading (see `envelope_predistort.h`'s REVISION 3 notes) — and it's exactly why `D` can never show any effect in this mode: there's no mapping stage left for it to correct. With `E` toggled on, the stepped value is instead treated as a commanded envelope index (same 0-1023 range/step size, so an existing automated harness driving `>`/`<`/`N`/`B` needs no changes beyond sending `E` once), run through `D`'s predistort LUT if it's on or the linear `u`/`j`/`i`/`k` offset/scale mapping if it's off — the same branch `dsp_task`'s normal per-tick path takes — before being converted to a duty count and written; each step then prints `-> env index NNN/1023 (frac=X.XXXX) -> duty NNN/1023 (predistort ON|linear mapping, 'D' off)`. The carrier/phase path is unaffected by either mode — select a steady source separately (`s`, single-tone, phase held rock-steady, is the natural choice — same as REVISION 1/2's own characterization used). Added directly to answer "check the distortion correction... put in loop (on/off) for the duty measurement ('d')" — see `group_delay_fit_notes.md`'s matching entry for the full rationale and why plain `d` mode couldn't do this on its own.

## Envelope-null floor

| Key | Effect |
|---|---|
| `x` | Raise the envelope-null floor by 0.02 |
| `z` | Lower the envelope-null floor by 0.02 (min 0.00) |

Two-tone envelopes dip toward zero at destructive-interference nulls, driving the commanded duty into the predistort LUT's steepest, most sparsely-characterized region (36–55% duty). `envelope_floor_apply()` (see `envelope_floor.h`) smoothly compresses envelope's whole `[0,1]` range into `[floor,1]` (peak still maps to 1, only the bottom is raised) so it can't be driven that low.

**Two earlier versions were tried and removed** — see `envelope_floor.cpp`'s header comment for both postmortems:

1. Also froze `freq_dev_hz` (phase rate) below the floor, meant to guard against `atan2()` noise near I=Q=0 — wrong for a two-tone signal (the fast phase rotation at a null is genuine required signal content, not noise) and produced a hard phase discontinuity: no effect until the floor got large enough (~0.08 in testing), then products jumping to 0dB right at that threshold as the accumulated phase debt snapped back.
2. A hard clamp (`min(envelope, floor)`) on the envelope side alone — left a derivative discontinuity at every crossing, which a two-tone signal hits often (near every null); showed up as nearly every product rising gently with each floor increment, a new distortion source confounding the actual question being tested. Replaced with the current continuous affine remap, which has no threshold and no kink anywhere in the trajectory.

Floor starts at 0.00 (off) — raising it trades some true-null carrier suppression (the deepest measured nulls only bought ~58dB anyway, see `envelope_predistort.h`) for keeping envelope out of the LUT's worst region. Applied unconditionally, right after `ssb_dsp_process_sample()`/the ENVSTEP/FMTEST/AMTEST equivalents, before `g`'s group-delay equalizer or `D`'s pre-distortion touch the envelope — so it sees the true raw signal, not a downstream-massaged version of it.

## freq_dev slew-rate limiter

| Key | Effect |
|---|---|
| `{` | Tighten the `freq_dev_hz` slew-rate limit (turns it on at 2000Hz/sample if currently off, then steps down 250Hz/sample at a time, floor 100Hz/sample) |
| `}` | Loosen the limit (steps up 250Hz/sample, then turns fully off past 8000Hz/sample) |

The phase-side counterpart to the envelope-null floor above — see `ssb_dsp.h`'s `ssb_dsp_set_freq_dev_slew_limit_hz()` doc comment for the full derivation. This limits how fast `freq_dev_hz` (the FM deviation fed to the AD9851 each sample) is allowed to *change* from one sample to the next — a different thing from `MAX_FREQ_DEV_HZ`, which limits its *value*. Verified numerically (a double-precision reimplementation of the exact DSP algorithm run alongside the real fast-math code, on synthetic two-tone input): real content, away from any envelope null, never asks `freq_dev_hz` to change faster than ~60Hz/sample even on busy high-center-frequency bands, while a genuine destructive-interference null asks for ~8000Hz/sample in a single step — a 100x+ gap with nothing in between. That single-sample swing is a real, correctly-computed requirement (it represents the true instantaneous phase reversal at the null), but a fast transient in frequency is inherently wideband in the spectrum. Limiting the slew trades a little reconstruction fidelity right at the null for less spectral splatter, without touching ordinary content.

Deliberately not a repeat of either envelope-floor mistake above: it doesn't freeze `freq_dev_hz` (no phase debt to snap back later — the AD9851 just integrates whatever frequency word it's handed each sample, so a slower-than-ideal ramp through the null *is* the applied modulation, not a deferred correction), and it isn't a hard value clamp (a slew limiter's output is continuous by construction, no derivative kink at any threshold). Applied inside `ssb_dsp_process_sample()`, after the `max_unclamped_freq_dev_hz` diagnostic captures the true unlimited peak and before the existing `MAX_FREQ_DEV_HZ` magnitude clamp (which still applies on top, unchanged).

Off by default (same convention as `g`/`D`/`x`/`z`) — not yet validated on real hardware beyond the numeric derivation above.

**2026-09-03: validated on real hardware — and the ~8000Hz/sample figure above was too low.** Tightening the limit below 8000Hz/sample made 3rd-order IMD worse on the bench, even the smallest step down — but loosening it *above* 8000, to 12000 then 20000, gained 1-2dB on 3rd-order IMD with nothing else getting worse. Re-simulated across many sample-grid phase alignments (not just one): the true worst-case single-sample swing at a null for this tone pair is closer to **~9640Hz**, not ~8000 — the earlier derivation missed that a swing crossing through zero (opposite-sign) can run close to double the size of the same-sign swing it checked. That's why 8000Hz/sample was already clipping the worst cases (hence tightening only made it worse) and why loosening helped. **Practical gap:** `FREQ_DEV_SLEW_MAX_FINITE_HZ` (the ceiling `'}'` steps up to before going fully off) is still 8000 in the firmware, so the empirically useful 12000-20000+ range can't be reached from `'{'`/`'}'` yet — needs raising. Full derivation in `group_delay_fit_notes.md`.

## Envelope output interpolation

| Key | Effect |
|---|---|
| `I` | Toggle 4x envelope output interpolation on/off |
| `C` | Cycle the interpolation curve: Catmull-Rom (default) -> linear -> HOLD (v4.4, no ramp) -> ... |

Right now the commanded PWM duty only changes once per DSP tick (`SAMPLE_RATE_HZ` = 16000) — a zero-order-hold staircase feeding the analog Sallen-Key reconstruction filter, whose first spectral image sits at 16kHz. Checked against the same real LTspice sweep behind `envelope_gdeq.h`: the filter only provides -27dB of attenuation at 16kHz, but -63dB at 64kHz — so linearly interpolating the envelope between DSP ticks up to 4x the update rate (64kHz) pushes that image out to where the filter actually buries it, +36dB "for free." Directly inspired by QRP Labs' QMX SSB firmware (see project history/`G0UPL.pdf`), which does the same thing (28x, driven by its own DAC/CPU constraints — 4x here is a deliberately modest first step, not a re-derivation of that number) and reported it eliminating visible overshoot/undershoot on the amplitude waveform entirely.

Implemented (v4, after three earlier real-hardware failures — see below) by speeding up the EXISTING sample timer itself to `ENVELOPE_INTERP_FACTOR` (4) x `SAMPLE_RATE_HZ` (64kHz), and only running the full DSP pipeline (Hilbert transform, atan2, sqrt, freq_dev, envelope, ADC, AD9851, diagnostics — everything) on 1 in `ENVELOPE_INTERP_FACTOR` of those wakes ("full" ticks, still true `SAMPLE_RATE_HZ` — every filter constant in the codebase keeps assuming that rate unchanged). The other 3 wakes just walk a linear-interpolation ramp between the last two full-tick envelope values and call the same plain `envelope_output_write_pwm()` the code always used. No second timer, no ISR, no extra task, no LEDC hardware fade — it all happens inside `dsp_task` itself, a single already-proven-safe FreeRTOS task on Core 0.

Took four attempts to land here, all from real-hardware failures, not simulation — recorded because each one rules out an approach that looks reasonable on paper: v1 (a second 64kHz `gptimer` whose ISR did the interpolation math *and* the `ledc_set_duty()`/`ledc_update_duty()` write directly) rebooted the ESP32 outright — general LEDC driver calls aren't guaranteed IRAM/ISR-safe. v2 (deferred just the write to a dedicated task, notified via `vTaskNotifyGiveFromISR`) still crashed, with a "Coprocessor exception" panic — the ISR still did the interpolation *arithmetic*, and on this chip's Xtensa cores the FPU is itself a coprocessor whose context only survives a *task* switch, so any float op in a true ISR is unsafe. v3 (moved the arithmetic into the task too, then rebuilt around the LEDC peripheral's own hardware duty-fade engine instead of any software timer) stopped crashing, but exposed two more problems in turn — a dedicated task waking 64,000 times/second on Core 1 starved `loop()`/`handle_serial_commands()` of CPU time; and once that was fixed via the hardware fade engine, the LEDC's free-running PWM clock (78125Hz, no integer relationship to `SAMPLE_RATE_HZ`) introduced ~12.8us of unsynchronized timing jitter per tick, visible as the envelope's effective frequency jittering. v4 (the user's own suggestion) sidesteps all of it by not using a second clock domain or extra task at all — `dsp_task` has always lived on Core 0, physically separate from `loop()`/Serial on Core 1, so running it faster carries none of v3's starvation risk, and there's only ever one clock. v4's own first cut still had a bug, found on real hardware EVEN WITH 'I' OFF (envelope timing jittery, IMDs shuffled up and down): it tracked "which of the 4 fast ticks is this" with a bare software counter incremented once per wake, but `ulTaskNotifyTake(pdTRUE, ...)` silently coalesces multiple real hardware ticks into one wake if `dsp_task` is ever even briefly late (a cache stall, a moment of contention) - at 64kHz that's a real possibility, and once it happens the counter permanently desyncs from the true hardware phase, so "full" ticks start firing at the wrong, irregular offsets indefinitely (a form of sample-clock jitter on the DSP rate itself). Fixed by using `ulTaskNotifyTake()`'s own return value (how many ticks actually elapsed, not just "at least one") to advance the counter, so it always equals the true cumulative hardware tick count and the phase can never drift. That fixed the OFF-state jitter, but the envelope still looked jittery with 'I' ON - a second, related bug (v4.1): the ramp itself was step-COUNTED (one `on_interp_tick()` call = one step), which is fragile against the exact same coalescing effect, now applied to the ramp - and it turns out to fire on almost every cycle, not rarely, because a full tick's own DSP processing (tens of us) is comparable to or longer than one fast sub-period (~15.6us), so 1-2 of the following interp ticks routinely elapse and coalesce away before dsp_task gets back to blocking, losing that many ramp steps by an amount that varies with exactly how long that tick's processing took - a real, structural, content-dependent source of jitter in the rendered envelope shape. Fixed by making the ramp time-BASED instead: each write computes its value directly from `esp_timer_get_time()` minus the true tick-start timestamp, divided by the sample period, rather than from a step count - so whatever calls DO happen (however many, however irregularly spaced) always land on the mathematically correct point on the ramp, and a coalescing event just means fewer visible steps that cycle, not a wrong value at the ones that do render. Still moving around after that (v4.2): the phase-lock fix only checked `fast_tick_count % ENVELOPE_INTERP_FACTOR == 0`, which assumes a coalescing event always lands EXACTLY on a multiple of 4 - it doesn't have to. If a full tick's own DSP processing occasionally takes long enough that MORE than 4 real periods elapse before `dsp_task` gets back to blocking, the count can jump straight past the next boundary (3 -> 8, skipping 4) - the modulo check then stays false until the count next happens to land exactly on a multiple, meaning that period's real DSP sample is skipped outright (a genuinely missing sample, not just a cosmetic ramp step), and the following full tick is delayed further still while waiting to re-land cleanly - compounding the original overrun instead of absorbing it. Fixed by comparing which `ENVELOPE_INTERP_FACTOR`-sized GROUP the count falls into rather than its exact remainder - any update that crosses one or more group boundaries is recognized as a full tick immediately, however far past the exact boundary a coalesced count landed, so a bad overrun still costs the one sample it made unrecoverable but can no longer cascade into delaying subsequent ones. Worth checking the existing `[timing]` diagnostic block's `overruns=`/`wakeup jitter: max_gap_us=.../late_ticks_total=` line - if those are climbing, `dsp_task`'s own per-tick processing time is what needs investigating next, separately from this counter logic. See `ssb_mic_test.ino`'s `dsp_task` and `envelope_interp.h` for the full design and failure history.

Off by default (same convention as `g`/`D`/`x`/`z`/`{`/`}`) — not yet validated on real hardware. **2026-08-31: real-hardware two-tone testing found `I` makes stability audibly *worse*, not better — a clear regression, not a subtle effect.** See `null_bias_investigation.md`'s item 5 (parked, higher priority than that document's own null-crossing-bias findings) — worth re-reading the v1-v4 failure history above first in case this is a fifth failure mode of the same kind, before assuming a connection to the separate null-crossing bias work.

**Curve, and `C` to switch it live.** The ramp between full-tick samples was originally a straight line (v4 above); `envelope_interp.h`'s own v4.2 revision replaced it with a Catmull-Rom cubic instead, validated offline against this project's own two-tone and AM-test envelopes before ever being flashed: ~23dB less interpolation error on the smooth AM-test envelope, but only ~2dB on the two-tone envelope specifically, because a two-tone null is a genuine derivative discontinuity (a local V-fold) that a smooth cubic structurally cannot reproduce — it rounds the fold off instead, where a straight line naturally can't do anything BUT reproduce a V. `C` (v4.3) makes that choice runtime-switchable for a direct A/B, without needing a reflash between them — see `group_delay_fit_notes.md`'s 2026-09-01 entries for why this became relevant (raised while chasing the new PNP BC327+attn filter's real-hardware IMD results). Both curves render over the exact same data window/timing (still the v4.2 4-sample/2-tick-latency pipeline) so switching `C` isolates curve SHAPE alone — no need to re-tune `relative_delay` between the two choices. Only affects output while `I` is ON; harmless to toggle with `I` off (remembered for next time `I` is enabled). Persisted per-preset as `envelope_interp_curve` (`settings.h`), defaulting to Catmull-Rom for every existing preset.

**Third curve, HOLD (v4.4) — isolates write RATE from ramp SHAPE.** `I` off vs on doesn't just add a ramp: with `I` off, `envelope_interp_on_interp_tick()` is a complete no-op, so the LEDC duty register is only written once per full DSP tick (`SAMPLE_RATE_HZ` = 16kHz effective PWM update rate). With `I` on — CATMULL_ROM or LINEAR alike — the register gets written 4x more often (64kHz), whatever curve is filling those extra writes. The Catmull-Rom-vs-linear comparison above only ever varied the SHAPE of that faster stream; it never isolated whether the faster write rate itself, independent of any ramp, is what matters. `C`'s third position, HOLD, answers that directly: it writes each full tick's value unchanged, 4 times, with zero added latency (unlike CATMULL_ROM/LINEAR, which both spend one tick "arriving" at the new value before settling). That gives a clean 3-way decomposition to A/B on the bench: `I` off (16kHz, once each) vs `I` on + HOLD (64kHz, same value repeated, no ramp) vs `I` on + CATMULL_ROM/LINEAR (64kHz, ramping) — isolating write rate from ramp shape instead of changing both at once. Same persistence/toggle rules as the other two curves (`envelope_interp_curve` in `settings.h`; harmless to leave selected with `I` off).

**2026-09-02, real-hardware result: HOLD raises the noise floor, both audibly and measured.** First bench use of the 3-way decomposition above found `I` on + HOLD produces a clearly higher and broader broadband noise floor than `I` off, heard directly and confirmed on a real-time FFT display (SDRuno, RBW 0.18Hz) — independently re-measured from the two capture screenshots themselves (pixel-brightness/lit-fraction of the off-peak skirt region, not just eyeballed): the HOLD capture ran ~30% more of that skirt area lit above threshold than the `I`-off capture, consistent across every off-peak sub-band checked, not a one-off artifact of a single frequency. Since HOLD writes a **bit-identical** duty value on all 4 of its per-tick writes, this rules out any explanation resting on the *value* changing — whatever is happening is triggered by the extra `ledc_set_duty()`/`ledc_update_duty()` calls themselves. Directly checked against the leading candidate mechanism (an LEDC internal latch-timing race, since `RSET_MOD_LEDC_FREQ_HZ`=78125Hz shares no integer relationship with the 64kHz write rate and the margin between the two is much tighter than at 16kHz): GPIO2 (`RSET_MOD_LEDC_GPIO`, the actual RSET/PWM line, pre-filter) scopes clean and equally well-behaved across `I` off, HOLD, LINEAR, and Catmull-Rom — no visible timing jitter or glitch on the commanded PWM waveform itself in any mode. That rules out a corrupted output waveform as the cause. Leading remaining hypothesis: digital switching/bus-activity crosstalk from the 4x-more-frequent register writes coupling onto some *other* node in the signal chain — the same category of effect as the confirmed `ADC_CONT_SAMPLE_FREQ_HZ`/`CHIRP_REF_GPIO`(pin13) crosstalk finding earlier this project, just now implicating the LEDC write path instead of the ADC driver. Not yet localized — next step is scoping the ADC input / audio chain or a ground/supply reference while toggling `I`/`C`, the same way the GPIO13 artifact was originally traced, to find where it's actually getting in.

**Correction, 2026-09-02 (same day): likely NOT an `I`-specific effect — retracted pending re-test.** Further bench work could not reproduce any `I`/HOLD-specific noise, and turned up a much bigger, better-explained cause instead (see "Sample rate (Fs) and dsp_task timing margin" below): at `SAMPLE_RATE_HZ`=16000 dsp_task is running with very little timing margin per tick, independent of `I` entirely. Worth also correcting the framing above: the fast-tick `gptimer` already fires at the full `ENVELOPE_INTERP_FACTOR`x rate (64kHz) **unconditionally**, regardless of whether `I` is on — see the sine-chirp test mode's own note in `config.h` ("the fast-tick infrastructure already runs unconditionally at 64kHz regardless of whether 'I' interpolation is toggled on"). So `I` off vs on was never "no wake-up at 64kHz" vs "wake-up at 64kHz" — dsp_task is woken at 64kHz either way; `I` only gates whether that wake's payload is a no-op or a few FLOPs + one register write. That's a small marginal CPU delta to hang a ~30%-more-lit noise floor on, which fits with it turning out not to reproduce. Most likely the two original screenshots simply landed on different sides of 16kHz's already-thin, already-variable margin, for reasons unrelated to `I`. Leaving the write-up above in place rather than deleting it (per this project's own convention of parking findings, corrections included) — the GPIO2-clean scope result is still valid and worth having on record either way.

**2026-09-02, settled: `I` left off (`ENVELOPE_INTERP_FACTOR` effectively x1) as the operating default, and `SAMPLE_RATE_HZ` back to 16000.** After the full HOLD/LINEAR/Catmull-Rom investigation above plus the separate factor 4/5/8 wake-rate sweep (below), real-hardware listening found no `I` setting — including x2, the mildest step up from off — that avoids an audible noise "burble" on tones, so there's no configuration where `I` provides its intended imaging benefit without a clearly audible cost on this hardware. Combined with the earlier `[timing]` margin findings (16kHz leaves ~16-17.5us of headroom vs 10kHz's ~55-58us) this closes out the interp/Fs investigation for now at the pre-existing, best-understood operating point: `I` off, `SAMPLE_RATE_HZ`=16000. Worth revisiting if the underlying mechanism (still unresolved — see the wake-rate-alone vs float-ramp-cost discussion above) ever gets pinned down and fixed.

**2026-09-07, reopened: "`I` off is clean" was never actually verified at the real 64kHz wake rate this default has been running at since `SAMPLE_RATE_HZ` went back to 16000 the same day.** User: "even setting x4 Fs for interp causes audible burble on the two tone with I-off." Not a contradiction — a gap. The decisive `'I'`-off-as-clean-baseline test above was run at `SAMPLE_RATE_HZ=10000`/factor=4 (40kHz wake); `ENVELOPE_INTERP_FACTOR` unconditionally drives the real `gptimer` rate regardless of the `'I'` runtime toggle (confirmed in `init_sample_timer()` — no gate on `'I'` anywhere), so the actual default wake rate ever since has been 64kHz (16000x4), never re-tested with `'I'` off specifically. Since `'I'` off is a confirmed bare no-op on interp ticks (`envelope_interp.cpp`, no `envelope_output_write_pwm()` call at all), every LEDC-write-rate theory is ruled out for this symptom — the remaining suspects are the `gptimer` ISR and cross-core notify/IPI firing 64,000x/sec by themselves, the same category of cost that caused the still-unexplained Core-1 co-location starvation earlier in the project. Recommended next steps, cheapest/most decisive first: (1) rebuild with `ENVELOPE_INTERP_FACTOR=1` for a true zero-overhead 16kHz-wake baseline — apparently never actually tried; (2) no-rebuild check, drop to `SAMPLE_RATE_HZ=10000` (restores the original 40kHz wake the decisive test used) and re-listen; (3) check `[timing]`'s `overruns`/`late_ticks_total` in the current burbling config to rule in/out a mundane deadline-miss explanation. Not yet run. Full derivation in `group_delay_fit_notes.md`'s matching entry.

**2026-09-07, correction + decisive result: "`I` off" has always meant `ENVELOPE_INTERP_FACTOR=1` historically (step 1 above already done), and a controlled 1-vs-4 retest confirms a real, independent wake-rate-alone burble, smaller than but distinct from the write-rate effect.** User: "Whenever I reported that I was running with I-off now as default is when I also set ENVELOPE_INTERP_FACTOR=1. Recently I retried =4 to check and burble was there albeit at a lower level then when enabling interp writes." So the 2026-09-02 "settled" baseline genuinely was clean (true 16kHz wake, not secretly 64kHz as speculated above) — but the fresh 1-vs-4 comparison (`'I'` off both times, zero LEDC writes either way) is the cleanest isolation yet: raising the wake rate ALONE, with no register write anywhere on the extra ticks, still produces real (if lower-level) burble. This rules out "register-write electrical crosstalk" as the whole story — there are now two independent, apparently stacking contributors: wake-rate-alone (gptimer ISR + cross-core notify/IPI overhead, likely perturbing the full tick's own AD9851/timing chain by a few us) plus an additional, larger write-rate cost on top when `'I'` is on. Refined next steps: the factor=1 baseline is done; still worth (a) checking whether the wake-rate-alone burble is present at `SAMPLE_RATE_HZ=10000`+factor=4 (40kHz) too, to bracket the threshold, and (b) checking `[timing]`'s overrun counters at the current config — though a clean reading wouldn't rule the mechanism out, since this looks like few-microsecond-scale jitter, likely too fine for that counter to catch. Full derivation in `group_delay_fit_notes.md`'s matching entry.

**2026-09-07, later still, bracket test conclusive: the wake-rate-alone burble is a real-time margin problem specific to 16kHz, not EMI/crosstalk.** User: "10kFs + x4 works, sweet tone and low noise floor immediately around tone. 16k+x4 -> burble, tone freq shifts, 10-15dB higher noise floor around tone." The "tone freq shifts" detail is decisive on its own: TWOTONE is a pure software oscillator with no external timing reference, so its output frequency can only shift if `dsp_task`'s full-tick calls happen at an irregular effective rate — direct, first-principles proof `dsp_task` is missing/mistiming its own deadline at 16k+x4, not a hypothesis. Recomputed the `gptimer` numbers directly: factor=4's `resolution_hz`=8MHz is a clean APB/10 divisor and `alarm_count` is an exact integer at both 10kHz (200) and 16kHz (125) — rules out a repeat of the factor=7 clock-rounding bug at either rate. The real difference is available real-time margin: 100us full-tick period at 10kHz (55-58us headroom, already on record) vs. 62.5us at 16kHz (only 16-17.5us) — the same fixed per-extra-wake overhead is comfortably absorbed at 10kHz and evidently isn't at 16kHz, intermittently pushing full ticks late enough to perturb the phase accumulators (freq shift) and inject sampling-jitter phase noise (the 10-15dB near-carrier rise — a well-understood DSP effect, not a new mechanism). Mundane, closed mechanism for the wake-rate-alone component: `ENVELOPE_INTERP_FACTOR=4` simply doesn't fit inside 16kHz's real-time budget on this hardware, while it fits comfortably inside 10kHz's. Doesn't change the separately-measured write-rate cost (`'I'` on at 10kHz+factor=4 already showed its own real HOLD/LINEAR/Catmull-Rom degradation on top of a clean wake rate) — a clean wake rate is necessary but not sufficient. Still worth pulling `[timing]`'s overrun counters at both rates to confirm with hard numbers, but the frequency-shift logic alone is solid. Forward-looking: 10kHz reopens interpolation as viable specifically at that base rate, not a general fix. Full derivation in `group_delay_fit_notes.md`'s matching entry.

## Sample rate (Fs) and dsp_task timing margin

**2026-09-02: reverting `SAMPLE_RATE_HZ` from 16000 to 10000 produced a dramatic, across-the-board improvement — bigger than anything `g`/`I`/`D` have shown so far, and it looks like the real explanation behind most of this session's noisier-than-expected real-hardware results.** Using Preset 1 (two-tone, `env_gdeq_enable`/`envelope_interp_enable` both off) and a mid-band noise test (0-3kHz broadband content relative to a 750Hz mid-band reference): at 16000Hz, -22dBc, and `D` (`env_predistort_enable`) made it 0.3dB *worse*. At 10000Hz: -32dBc with `D` off (a 10dB improvement from Fs alone) and -38dBc with `D` on (`D` now worth a genuine +6dB, instead of being net-negative). Frequency stability and the tone's subjective quality were both reported as noticeably better at 10kHz too. The tradeoff is real and expected: PWM/ZOH imaging is worse at 10kHz (closer first image, less filter stopband margin) — the whole reason `SAMPLE_RATE_HZ` was raised to 16000 and `envelope_interp`'s x4 output interpolation was built in the first place.

Why this is very likely a genuine timing-margin story, not a coincidence: `config.h`'s own `SAMPLE_RATE_HZ` comment already has the numbers. At 10000Hz (100us period), measured per-tick work (adc+dsp+write) was ~45us, leaving ~55us of margin — comfortably above the "~12-20us of wakeup jitter already measured" noted in that same comment. At 16000Hz (62.5us period), that same ~45us of work leaves only ~17.5us of margin — barely above that *same* 12-20us jitter figure, i.e. close to the exact threshold that got 20000Hz rejected outright in that comment ("period drops to 50us, leaving too little margin"). 16000 was picked as "the next value up from 10000 that still divides evenly," not because its margin was independently re-validated as generous — and on this data it wasn't. A tick that occasionally misses that ~17.5us window delays the next AD9851 frequency-word update, which is real, direct FM/PM timing jitter on the signal itself — a mechanism that would show up exactly as elevated broadband noise and worse frequency stability, and wouldn't touch `D`'s effectiveness directly (a static envelope-to-duty LUT correction has nothing to do with tick timing) except by being swamped underneath it, which is exactly the pattern seen (`D`'s benefit reappears once the swamping noise is gone at 10kHz).

**2026-09-02, `[timing]` captures at both rates (Preset 1, TWO-TONE, `I`/`g` off, `'r'` reset first):** at 16000Hz, `max_busy_us=46 (adc=4 dsp=20 write=28) period_us=62 overruns=0`, `max_gap_us=75 (nominal=62) late_ticks_total=0`. At 10000Hz, `max_busy_us=42 (adc=5 dsp=16 write=24) period_us=100 overruns=0`, `max_gap_us=107 (nominal=100) late_ticks_total=0`. Neither shows a formal overrun or late tick, so the strong "dsp_task is missing its deadline" framing isn't directly confirmed — but the margin actually available is: 16us at 16kHz vs 58us at 10kHz (matching the config.h estimate almost exactly), and the worst observed wake-gap excess eats ~80% of that margin at 16kHz (13us of 16us) vs only ~12% at 10kHz (7us of 58us). 16kHz is running close to its own edge even when nothing formally trips the overrun counter; 10kHz has a large cushion. Also notable: `[dsp] freq_dev: max_unclamped` dropped from 6697Hz (16kHz) to 3697Hz (10kHz) — consistent with `diagnostics.cpp` computing instantaneous frequency as `dphi * SAMPLE_RATE_HZ / (2*pi)`, meaning any *fixed* amount of phase-domain noise (quantization, ADC noise, Hilbert/atan2 rounding) gets scaled up proportionally by Fs before it ever reaches the AD9851 — a second, smooth, deterministic mechanism independent of scheduling jitter, plausibly worth a few dB on its own via FM-sideband scaling (~20*log10(16000/10000) ≈ 4dB).

**2026-09-02, `ENVELOPE_INTERP_FACTOR=7` at `SAMPLE_RATE_HZ=10000` — retracted, wrong fix for the wrong reason.** The idea (see the now-superseded item 4 this replaces) was to decouple the heavy pipeline's rate from the PWM output rate: keep the full pipeline at 10kHz for margin, but raise the interp factor so the envelope's PWM write rate could still reach ~70kHz for imaging. On the bench this brought the noise straight back to the same poor level as plain 16kHz. That result on its own looked like it might mean *wake rate itself* (not the full pipeline's Fs) was the real culprit — but a cleaner, fully-controlled follow-up test at a *fixed* wake rate ruled that out too, and points somewhere much more specific and useful:

**2026-09-02, decisive: it's not wake rate, it's floating-point ramp computation on the interp tick.** All four readings below are at the *same* wake rate — `SAMPLE_RATE_HZ=10000`, `ENVELOPE_INTERP_FACTOR=4` (40kHz), Preset 1, `D` on, mid-band noise test (`Pwr mid 750Hz band`, higher/less-negative = worse):

| Curve | Result | Δ from baseline |
|---|---|---|
| `I` off (baseline) | -35.4dB | — |
| `I` on, HOLD (no float math, frozen value) | -33.4dB | +2.0dB |
| `I` on, LINEAR (one multiply-add, changing value) | -23.9dB | +11.5dB |
| `I` on, Catmull-Rom (cubic Hermite, changing value) | -21.2dB | +14.2dB |

LINEAR sits far closer to Catmull-Rom (2.7dB apart) than to HOLD (9.6dB apart), despite being the much simpler of the two real curves — a single multiply-add vs a full cubic Hermite evaluation. That rules out "more arithmetic = proportionally worse": if raw operation count were the driver, LINEAR should land roughly midway between HOLD and Catmull-Rom, not almost co-located with the expensive one. What actually separates HOLD from both real curves is binary, not a matter of degree: HOLD never touches the FPU and always (re)writes the exact same frozen duty value; LINEAR and Catmull-Rom both do real floating-point work and genuinely change the commanded duty on every interp tick. Whichever of those two properties is the trigger, it looks like a step function at that threshold, not a smooth cost curve.

**Correction, 2026-09-02 (same day): the `ENVELOPE_INTERP_FACTOR=7` result is a SEPARATE bug, not the same mechanism as the table above.** Re-tested with `I` fully OFF at factor=7 (10000Hz base) — still -21.174dB, essentially identical to the factor=4 Catmull-Rom result (-21.181dB) despite the interp tick being a guaranteed no-op with `I` off. That rules out float-ramp cost or a changing duty value as the cause at factor=7 specifically, since neither happens with `I` off. The real cause: `init_sample_timer()` sets `resolution_hz = 2000000UL * ENVELOPE_INTERP_FACTOR` — 8MHz at factor=4, 14MHz at factor=7 — while `alarm_count` stays a fixed `2000000UL / SAMPLE_RATE_HZ` (200), computed assuming the requested resolution is achieved exactly. `GPTIMER_CLK_SRC_DEFAULT` on this chip is the 80MHz APB clock (same assumption this project's own LEDC comment already makes elsewhere). 80MHz/8MHz=10 is a clean integer prescaler; 80MHz/14MHz≈5.714 is not, so the driver silently rounds to the nearest achievable resolution — almost certainly 80MHz/5=16MHz (a +14.3% error), not the requested 14MHz. Confirmed directly: `[dsp] long-window avg=11371.71 ticks/s (13.717% vs nominal 10000Hz) true_ratio=7.0914 (nominal=8)` — a real, hardware-timestamped measurement (via `esp_timer_get_time()`, independent of the gptimer's own assumptions) showing dsp_task's actual full-tick rate running ~13.7% fast, matching the predicted +14.3% resolution error closely enough (small residual plausibly the ADC's own independent +0.8% clock error, plus measurement-window noise) to call this confirmed. At factor=4, this same diagnostic line has consistently read ~0.000-0.008% off nominal — 8MHz's clean divisor avoids the problem entirely.

Consequence: at factor=7, *everything* downstream of `SAMPLE_RATE_HZ` — the Hilbert/atan2 phase-to-frequency conversion (`dphi * SAMPLE_RATE_HZ / 2*pi`), the AD9851 write cadence, the ADC decimation ratio (`ADC_SAMPLES_PER_TICK` assumes an exact 80000/10000=8 relationship, and the diagnostic's own `true_ratio=7.09 (nominal=8)` shows that assumption is now wrong too) — is being computed against a `SAMPLE_RATE_HZ` constant that no longer matches the tick rate actually being delivered. That's a severe, systematic mistuning of the whole pipeline, entirely independent of `I`/curve choice, which is exactly why it showed up "even with `I` off." This has nothing to do with the HOLD/LINEAR/Catmull-Rom finding above — that one was cleanly demonstrated entirely at factor=4, a clock-clean rate, so it remains a real, separate, still-open question.

Two consequences for next steps: **(1)** the previously-proposed "factor=7 + HOLD" isolating test (see below) is now moot as a clean test of the compute-cost question, since factor=7 itself is broken for an unrelated reason — any result there would be confounded by this clock error, not informative about float-ramp cost. **(2)** if a higher PWM update rate than 40kHz is still wanted at `SAMPLE_RATE_HZ=10000`, pick a factor that divides cleanly: `80,000,000 / (2,000,000 * factor)` needs to be an integer, i.e. `40 / factor` needs to be a whole number — factor ∈ {1, 2, 4, 5, 8, 10, 20, 40} are clean (7 wasn't one of them), so factor=5 (50kHz) or factor=8 (80kHz) would be genuinely clock-accurate choices worth trying instead, once/if the separate compute-cost question below is resolved in a way that makes a higher factor worth having. Also worth fixing at the code level regardless: `init_sample_timer()` should read back the gptimer driver's *actually achieved* resolution (ESP-IDF's `gptimer_new_timer()` reports this) and derive `alarm_count` from that instead of assuming the request was granted exactly — that would make any future `ENVELOPE_INTERP_FACTOR` choice safe by construction rather than silently wrong for the "unclean" ones.

Two candidate mechanisms remain for the still-open HOLD-vs-LINEAR/Catmull-Rom finding (factor=4, clock-clean, so unaffected by the above), not mutually exclusive, both consistent with a step-function-at-threshold pattern rather than a smooth compute-proportional one:
- **CPU/scheduling:** the ESP32-S3's FPU is a genuine coprocessor (this project already hit that wall once, in the v1/v2 `envelope_interp` failure history — see above). If the *first* FPU touch after a context switch carries a fixed save/restore cost, any float ramp (LINEAR or Catmull-Rom) would pay roughly the same large fixed penalty per call regardless of how many operations follow, while HOLD's FPU-free path never pays it at all — matching the data far better than a compute-proportional story would.
- **Analog/RF:** HOLD's duty value is electrically static across all 4 of its per-full-tick writes — indistinguishable from `I` off except for redundant identical LEDC writes. LINEAR/Catmull-Rom genuinely move the duty (real switching-edge position change) on every interp tick, which could carry inherently more RF-relevant switching energy regardless of CPU timing — a real, expected cost of genuine modulation rather than a bug, and independent of anything CPU-side. The GPIO2 scope check from the (retracted) HOLD investigation found the PWM line "clean" across all curves — but that check has not yet been repeated specifically under this two-tone/Preset 1 content with a properly *dynamic* envelope; a near-constant test signal would hide exactly this effect since LINEAR/Catmull-Rom's ramp would have little to interpolate either way.

Not yet done, in order of how cheap they are: (1) re-scope GPIO2 comparing HOLD vs LINEAR/Catmull-Rom under this exact two-tone/Preset 1 condition at factor=4 (clock-clean), to distinguish the CPU-side hypothesis (would show timing jitter on the PWM line) from the analog/RF-side one (would show a clean but genuinely more-modulated waveform); (2) a lightweight timing probe directly around `envelope_interp_on_interp_tick()`'s curve branches (max/avg execution time per call, broken out by curve), to quantify the CPU-side hypothesis directly instead of inferring it.

**2026-09-02, item (3) done — HOLD itself degrades with wake rate alone, even with zero float math (curve state assumed, pending confirmation).** Re-tested `SAMPLE_RATE_HZ=10000` with the two remaining clock-clean factors above 4, presumed still on HOLD (this was the specific next step proposed above; not explicitly re-stated in the terse result messages, so treat as provisional until confirmed):

| Factor | Wake rate | Result | Δ from `I` off baseline (-35.4dB) |
|---|---|---|---|
| 4 | 40kHz | -33.4dB (HOLD, confirmed) | +2.0dB |
| 5 | 50kHz | -29.929dB (HOLD, assumed) | +5.5dB |
| 8 | 80kHz | ~-23dB (HOLD, assumed) | +12.4dB |

If the curve assumption holds, this is a clean, roughly monotonic degradation with wake rate alone — 40k→50k→80k — on a payload that never touches the FPU and rewrites the same frozen duty value every interp tick. That's a real, independent cost of high wake rate by itself, additive on top of the separately-confirmed (and larger, at a fixed rate) float-ramp/changing-value cost from the table above. It also **weakens the CPU/FPU-coprocessor-context hypothesis**: that theory predicted HOLD's FPU-free path should be largely immune to wake-rate scaling, but here it isn't. The analog/RF hypothesis doesn't fit cleanly either, since HOLD's duty value is static — redundant identical writes, not genuine modulation — so a third, more generic mechanism looks more likely now: something that scales with *ISR/wake frequency itself*, independent of what work happens inside each wake (e.g. fixed per-interrupt overhead — vector entry/exit, LEDC register write timing relative to the 78125Hz LEDC carrier repeating more densely, digital switching noise from the wake activity itself). This needs the curve state confirmed, and ideally isolated from the earlier float-ramp finding with a dedicated HOLD-only rate sweep at 4/5/8 run back-to-back in one session, before it's treated as settled.

**Interim recommendation:** leave `I` off, and if `I` is used, prefer the lowest clock-clean factor that gives useful imaging (4, i.e. 40kHz) rather than reaching for 5 or 8 to chase a higher PWM rate — both the float-ramp cost (fixed rate) and, provisionally, the wake-rate-alone cost (HOLD) get worse as factor increases, so there's no free lunch in going higher. At the most comfortably-margined case measured (`I` off, 40kHz-equivalent wake, 58us of headroom, zero overruns), `I` on still costs ~10-14dB even in its cheapest (HOLD, factor=4) form — an order of magnitude bigger than the "+36dB for free" imaging benefit it was built to capture ever needed to be weighed against, and bigger than any of `g`/`D`'s effects measured this session. Until the mechanism above is pinned down (and fixed, if it's addressable), `I` isn't paying for itself on this hardware.

## GPIO13 5kHz pulse — confirmed source: the ADC continuous driver

**2026-09-02, reported:** a regular short pulse on GPIO13 (`CHIRP_REF_GPIO`) at exactly 5kHz while in `'w'` (sine-chirp) test mode.

**2026-09-02, ruled out — not a software pin conflict.** GPIO13 is shared three ways in this codebase: `TIMING_DEBUG_GPIO_ADC` (adc_capture.cpp's ISR debug toggle, gated `ADC_ISR_DEBUG_PIN_ENABLED=0`), `TIMING_DEBUG_GPIO_CMD` (the `.ino`'s serial-activity marker, gated `CMD_DEBUG_PIN_ENABLED=0`), and `CHIRP_REF_GPIO` (the chirp mode's own intended square-wave reference output, unconditional). Checked every `#if` site touching this pin in `adc_capture.cpp` and the `.ino`: both debug-toggle paths are confirmed fully compiled out (single, unambiguous `#define`s, no override elsewhere in the project) — genuinely absent from the binary, not just runtime-disabled. So nothing in this project's own code was deliberately writing pin13 outside of the chirp reference toggle itself.

**2026-09-02, root cause identified: exact rate match to the ADC continuous driver's ISR.** `ADC_CONT_SAMPLE_FREQ_HZ / ADC_CONT_FRAME_SAMPLES = 80000 / 16 = 5000Hz` — precisely `adc_conv_done_cb()`'s own DMA-frame-completion firing rate (adc_capture.cpp), which runs continuously in every audio-source mode, chirp included, because `adc_capture_init()` is called unconditionally in `setup()` ("Always started now, regardless of the initial audio source"). Working theory: electrical coupling from the ADC continuous driver's real DMA/ISR activity onto the GPIO13 pad, independent of any code write to that specific pin — consistent with this project's own earlier, opposite-direction finding that driving GPIO3 digitally silently zeroed ADC conversions outright (config.h's "TRIED GPIO3 FIRST, REVERTED" note), which already established that GPIO1-10's digital activity and the ADC1 front-end aren't cleanly isolated on this chip. GPIO13 was deliberately chosen as ADC2 (unused by this project) specifically to get away from that — this suggests the isolation doesn't fully hold in the reverse direction either, plausibly because ADC1/ADC2 share analog reference/bias circuitry inside the SAR ADC block despite being logically separate units.

**2026-09-02, added `ADC_CAPTURE_ENABLED` (config.h, defaults to 1, no behavior change) as an isolation test.** Guards all three real call sites in the `.ino`: `adc_capture_init()` in `setup()` (skips starting the driver entirely — `adc_continuous_start()` is never called and the ISR never registers, not just a muted debug pin), `adc_capture_service()` in `loop()`, and the mic-source sample read in `dsp_task` (falls back to `sample = 0.0f`, matching the existing AMTEST/FMTEST/ENVSTEP "unused" convention). `diagnostics.cpp`/`serial_commands.cpp`'s ADC accessor calls were left unguarded — they only touch static state inside `adc_capture.cpp` and are safe to call with the driver never started (report zeros).

**2026-09-02, confirmed on real hardware: `ADC_CAPTURE_ENABLED=0` kills the 5kHz pulse on GPIO13.** Decisive — the ADC continuous driver's DMA/ISR activity is the source, not the chirp reference code, not a residual debug-pin write.

**Implication worth testing next:** the ADC continuous driver has been running at this same 5kHz ISR rate, unconditionally, through *every* mid-band noise measurement so far this session (two-tone mode included — the mic FIFO isn't drained in that mode, but the driver and its ISR are still live per the "Always started now" comment above). If this same coupling mechanism reaches the analog envelope/RF path the way it reaches GPIO13, it could be a constant background contributor sitting underneath the whole HOLD/LINEAR/Catmull-Rom and factor 4/5/8 noise-floor investigation above — present equally in every condition tested, so it wouldn't explain the *differences* between them, but it could explain part of why even the best `I`-off baseline (-35.4dB) isn't lower. Worth a quick check: rerun the mid-band noise test (Preset 1, `I` off, `D` on, two-tone) with `ADC_CAPTURE_ENABLED=0` and compare against the existing -35.4dB baseline — a rise (less-negative, quieter) would indicate the ADC driver is also coupling into the RF path itself, not just the digital GPIO13 line.

**2026-09-02, pin catalog and root-cause reclassification: this is a general ADC-pin-class problem on this board, not a GPIO13-specific one.** Cataloging every pin actually in use (`config.h`/`carrier_output.h`/`envelope_output.h`) against the S3's ADC1 (GPIO1-10) / ADC2 (GPIO11-20) channel map: GPIO2/4/5/6 (RSET PWM, the two timing-debug pins, mic) are ADC1; GPIO9/10 (`AD9851_PIN_RESET`/`_DATA`) are ADC1; GPIO11/12 (`AD9851_PIN_FQUD`/`_WCLK`) are ADC2; GPIO13 was ADC2. Every free pin on the board's main 1-13 header (1, 3, 7, 8) is also ADC1 — there is no non-ADC pin available there at all. Between GPIO3 (driving it digitally zeroed ADC1 conversions outright) and GPIO13 (receiving coupling from the ADC driver's own DMA/ISR activity), this project has now found real-hardware evidence of trouble in *both directions* across *both* ADC units on this chip — confirming Espressif's own guidance (ADC-183 errata: ADC2's digital/DMA controller is documented as unreliable; hardware design guidelines state "ADC1 is recommended for use") without needing to trust it blindly, since both directions are now independently reproduced on this exact board.

Flagged, not yet tested: **GPIO9-12 (the AD9851 bit-bang interface) are the strongest untested candidate for this same class of problem**, arguably more than GPIO13 ever was — they sit on both ADC units (9/10=ADC1, same unit as the mic; 11/12=ADC2), and unlike GPIO13's low-duty-cycle debug/reference role, they're the busiest digital lines on the whole board: DATA/WCLK/FQUD/RESET all bit-bang every full tick, already documented as the single largest `[timing]` line item (~24-28µs). Two distinct risk paths: (1) the same ADC-driver-coupling direction as GPIO13, but onto GPIO9/10 specifically — though this wouldn't reach the RF output in two-tone/chirp mode since those modes never read the ADC FIFO for signal content; (2) direct board-level crosstalk from these fast lines onto the nearby analog envelope/PWM output (GPIO2) or mic input (GPIO6), which doesn't go through the ADC's digital pipeline at all and would be live in *every* mode, RF-relevant regardless of test source. Not rewired — these are point-to-point wired to the physical AD9851 module, a materially bigger change than swapping a debug pin, so this needs bench evidence (scope GPIO6 grounded, or the GPIO2 PWM line, alongside AD9851's WCLK/DATA for an edge-correlated glitch) before it's worth disturbing a working interface.

**2026-09-02, moved `TIMING_DEBUG_GPIO_ADC`/`TIMING_DEBUG_GPIO_CMD`/`CHIRP_REF_GPIO` from GPIO13 to GPIO39.** Board is an ESP32-S3 Super Mini: GPIO1-13 on the main header (all ADC-associated, per the catalog above), GPIO39-48 reachable via small solder pads. Within that pad range, GPIO43/44 are the UART0 console (reassigning discouraged), GPIO45/46 are two of the S3's four strapping pins, GPIO47/48 are already wired to the I2C DAC (`MCP4725_SDA`/`SCL_GPIO`) — leaving GPIO39-42 (default hardware JTAG TCK/TDO/TDI/TMS, unused and safe to repurpose since this project debugs over USB CDC serial, not external JTAG). Picked GPIO39. No ADC channel on the S3, not a strapping pin, not the console UART, not already claimed — the first debug/reference pin on this board with no ADC association at all. Code updated (`config.h`, `ssb_mic_test.ino`, `adc_capture.cpp`, `serial_commands.cpp`, `settings.h`, `test_signals.h`) to match; not yet re-verified on the bench that GPIO39 is itself clean (should scope it the same way GPIO13 was scoped, now that it's carrying the chirp reference again).

**2026-09-02, reported and root-caused: GPIO39 read permanently LOW.** Real bug, not a wiring/board issue — `GPIO.out_w1ts`/`out_w1tc` (used at every raw-register toggle site in `adc_capture.cpp` and the `.ino`, chosen specifically for their ISR-safety/zero function-call-overhead) are only 32 bits wide and physically address GPIO0-31 only. `1UL << 39` doesn't hit a real bit in that register at all, so `GPIO.out_w1ts = (1UL << CHIRP_REF_GPIO)` for pin 39 was silently a no-op — not undefined/crashing, just quietly doing nothing, leaving the pin stuck at whatever `pinMode`/`digitalWrite(...LOW)` left it during `setup()` (that init call goes through Arduino's `digitalWrite()`, which *does* handle GPIO32+ correctly - only the hot-path raw-register toggles were broken). This was invisible with the pin at GPIO13 (<32, correct register) and only surfaced once the pin moved to GPIO39 (>=32) - a latent bug the GPIO13→GPIO39 move exposed rather than one it introduced.

**Fix:** added `GPIO_FAST_SET(pin)`/`GPIO_FAST_CLR(pin)` macros (`config.h`, right after `TIMING_DEBUG_GPIO_ISR`'s definition) that branch between `GPIO.out_w1ts/w1tc` (pin < 32) and `GPIO.out1_w1ts/out1_w1tc` at bit `(pin-32)` (pin >= 32) - the S3's second GPIO bank, needed for anything past GPIO31. Since every pin argument here is a `#define` constant, the branch folds away entirely at compile time (zero runtime cost, still IRAM/ISR-safe) - this can't silently break again the next time a debug/reference pin gets reassigned to something >=32. Replaced all three real call sites: `on_timer_alarm()`'s pin5 toggle (didn't need the fix, converted anyway for consistency), the chirp reference toggle (the one that was actually broken), and `adc_conv_done_cb()`'s pin toggle (currently compiled out via `ADC_ISR_DEBUG_PIN_ENABLED=0`, but fixed for correctness whenever it's re-enabled next).

**2026-09-02, build failure on first attempt: `GPIO.out1_w1ts`/`out1_w1tc` are anonymous unions, not plain integers, on this SDK (`esp32s3-libs 3.3.8`).** Real compiler error: `no match for 'operator=' (operand types are 'volatile gpio_dev_s::<unnamed union>' and 'long unsigned int')`, on both branches, even for the pin5 call site that never actually takes the `>=32` path at runtime - because both branches of the `if`/`else` still have to type-check at compile time regardless of which one a constant pin number folds to. `GPIO.out_w1ts`/`out_w1tc` (low bank) accept a bare assignment fine on this SDK (already proven, left untouched); `GPIO.out1_w1ts`/`out1_w1tc` (high bank) need the union's `.val` member - `GPIO.out1_w1ts.val = mask` instead of `GPIO.out1_w1ts = mask`. Fixed and left a comment explaining why the two banks are treated differently in the same macro, so it doesn't look like an inconsistency to a future reader.

## Audio processing

| Key | Effect |
|---|---|
| `e` | Toggle EQ (HPF + presence peak) on/off |
| `c` | Toggle compressor on/off (automatic makeup gain applied) |
| `+` | Master gain +1.0dB |
| `-` | Master gain -1.0dB |
| `.` | Master gain +0.1dB (fine step) |
| `,` | Master gain -0.1dB (fine step) |

Master gain scales the *whole* chain (phase + envelope together, inside `ssb_dsp`) — different from the PWM range knobs above, which only touch envelope. `.`/`,` give 0.1dB resolution for dialing in precise gain-sweep measurement points (e.g. re-measuring `envelope_predistort.h`'s calibration table at finer steps) without 10 presses of `+`/`-` per dB.

## RF output

| Key | Effect |
|---|---|
| `o` | Toggle AD9851 RF output on/off (clean power-down via the chip's own control bit — preserves last-set frequency) |

## ADC / diagnostics

| Key | Effect |
|---|---|
| `f` | Cycle the ADC anti-alias low-pass filter: off → Butterworth → Chebyshev → off. Both are 4th-order (two cascaded biquads), same 3kHz -3dB point; Chebyshev trades a small in-band ripple (~1.4dB) for noticeably more stopband rejection above cutoff — see `adc_capture.h`/`ssb_adc_filter.h`. For A/B testing whether an artifact comes from the filter, and which filter family/order helps most |
| `v` | Mute/unmute the once-per-second `[timing]`/`[adc]`/`[dsp]` diagnostic block — mute this before adjusting other settings if you want to actually see the confirmation lines |
| `r` | Reset all diagnostic counters/watermarks for a clean measurement window (doesn't touch any of the settings above, only the stats) |
| `n` | Cycle the null-bias diagnostic's near-null envelope threshold through `{0.02, 0.05, 0.10, 0.20}` (starts at 0.05) — see the section below |

### Null-crossing frequency bias diagnostic (`[dsp] null_bias*`)

Added 2026-08-31 while chasing a two-tone "poor tone"/slow-drift/FM-noise complaint. Full derivation, method, and the confirmed measurement table live in **`null_bias_investigation.md`** — short version: `wrap_pi()`'s resolution of the ±π phase jump at each two-tone envelope null carries a small, deterministic, tone-pair-specific bias, and it's now measurable directly from firmware, no SDR needed.

Three lines print alongside `[dsp] freq_dev`, same ~1s cadence, gated the same way:

| Line | Fields | Use |
|---|---|---|
| `null_bias` | `f1`, `f2`, `expected_center` (`(f1+f2)/2`), `plain_mean`, `plain_bias` | `plain_bias` is a plain unweighted average — **confirmed wrong**, overstates the real effect by 2-3 orders of magnitude (100s of Hz vs. the real few-to-tens-of-Hz effect). Kept only for the mechanistic breakdown in `null_bias3`. |
| `null_bias2` | `weighted_mean`, `weighted_bias` | **The one that matches reality.** Envelope²-weighted average instantaneous frequency — the physically correct quantity (equals the power spectrum's centroid, a standard identity), confirmed against the SDR to within a few Hz across all six `TWOTONE_BAND_PRESETS` pairs. |
| `null_bias3` | `near_null_samples%`, `near_null_contrib`, `rest`, `threshold` | Diagnostic-only: how much of `plain_bias` comes from the small fraction of samples sitting right at a null (`envelope < threshold`) vs. everywhere else. Confirms the effect is localized to null crossings. |

Workflow to reproduce a reading: `T` to a tone-pair preset, confirm `e`/`c` (EQ/compressor) are off, confirm threshold reads 0.050 (press `n` to cycle back if not), `r`, wait ~1-2s, read the three lines. Counters reset via `ssb_dsp_reset_freq_dev_stats()` — same call `r` already makes, alongside `[dsp] freq_dev`'s `max_unclamped`/`clip_count`.

## Presets

| Key | Effect |
|---|---|
| `0`-`9` | Load a preset from `settings.h` — sets every lever above (audio source, relative delay, PWM offset/scale, gdeq, ADC LPF mode, EQ, compressor, master gain, RF output, envelope pre-distortion, envelope-null floor, freq_dev slew-rate limit, envelope output interpolation, interpolation curve) in one command. Boot banner lists the current names (dynamically, from the array's own size — always up to date). |
| `P` | Print the current value of every one of those same levers as a single comma-separated line, wrapped in `{ ... },` and in exactly `PersistentSettings`'s field order — copy/paste it straight into the `settingsPresets[]` array in `settings.h` as a new preset. Rename the placeholder `"Live"` name (and add a numbered comment above it, matching the existing presets' style) after pasting. |

Edit the `settingsPresets` array in `settings.h` to change them, or dial in levers live and use `P` to generate the line instead of hand-typing values — check the live boot banner or `settings.h` itself for the current name/count rather than this doc, since the preset list changes often during active tuning. There's a compile-time check tying the array size to the `'0'`-`'9'` range (`settings.h`'s `static_assert`), so resizing it without updating `loop()`/`setup()`/`serial_commands.cpp`'s preset-select block — and the boot banner's own preset listing loop, which now reads the array size directly rather than a hardcoded count — fails the build instead of silently misbehaving.

## Current testing defaults (compiled in, before any preset is loaded)

- Two-tone mode on at boot
- EQ and compressor both off
- Master gain: -2dB
- Relative delay: 0 samples
- Envelope group-delay equalizer: off
- `MAX_FREQ_DEV_HZ`: 8000Hz. Originally raised from 2800Hz just to measure the real unclamped two-tone peak deviation (`[dsp] max_unclamped`, see `diagnostics.cpp`) while chasing a ~+100Hz two-tone frequency offset; that measurement came back ~4800-4900Hz — well above the old 2800Hz ceiling, meaning that clamp was routinely engaging on real signal peaks, not just as an edge-case safety limit. Separately, real-hardware mic white-noise testing showed the freq-dev clamp performs better set higher, so **8000Hz is being kept for now** rather than reverted to 2800Hz — not yet landed on a final permanent value. UPDATE 2026-08-31: `clip_count` reads 0 at every tone-pair/spacing tested at this clamp setting, which rules clamp-asymmetry out as the cause of the (much smaller, current-firmware) two-tone offset seen now — see `null_bias_investigation.md` for the mechanism that replaced this as the leading explanation.

## Quick workflow reminders

- Send `v` first if you're about to do fine-tuning — otherwise confirmations get buried in the 1-second diagnostic spam.
- Levels take a moment to settle on the spectrum analyzer after any change — wait before reading.
- `[timing]` line's `mode=` field tells you which signal source is actually active (goes through the same name mapping used for `t`/`s`/`p`/`y`/`h`'s own confirmation lines, so `ENVSTEP`/`FM TEST`/`AM TEST` show up correctly there too, not just `TWOTONE`/`SINGLETONE`/`mic`), and `gdeq=` tells you whether the group-delay equalizer was on during that measurement window.
- Recommended workflow for validating `g`: `p` (envelope step) with `g` off vs on, scope the RSET step edge directly — flatter group delay should show as a cleaner edge. Re-tune `[`/`]` (starting near +2.65 samples) before judging real two-tone/IMD results with `g` on. `h` (AM isolation) is also worth an occasional off/on `g` check, though as of 2026-09-07 it's no longer flagged as a priority — see that dated entry above for why (the asymmetric AM-to-PM signature this note used to warn about isn't currently reproducible).
- Empirically mapping envelope/phase delay mismatch vs. frequency (no lab equipment needed beyond the RF spectrum analyzer already in use): `T` to step to a band, `[`/`]` to find the delay that minimizes IMD/spurs for that band, capture a spectrum, repeat for the next `T` press. The resulting per-band "best delay" values trace out the actual (as-built) delay-vs-frequency curve, which can be fed back into refitting `ENV_GDEQ_A1`/`ENV_GDEQ_A2` against real measured data instead of the original LTspice simulation.
